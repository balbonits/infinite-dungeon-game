# Development Tracker

Current state of the game. Updated as work completes. Run `make build` to verify the project compiles.

## How to Read This

- **Done** = implemented and tested (or verified visually)
- **Partial** = exists but incomplete or has known gaps
- **To Do** = not started
- **Priority**: P0 = blocking, P1 = next up, P2 = planned, P3 = future

---

## Phase 0 -- Visual Foundation (ALL DONE)

| ID | Title | Status |
|----|-------|--------|
| VIS-01 | Render one floor tile correctly | Done |
| VIS-02 | Render a tile room (10x10) | Done |
| VIS-03 | Place a character sprite on the tiles | Done |
| VIS-04 | Move the character with arrow keys | Done |
| VIS-05 | Camera follows the character | Done |
| VIS-06 | Place one enemy sprite that chases | Done |

---

## Phase 0.5 -- Playable Prototype (ALL DONE)

| ID | Title | Status |
|----|-------|--------|
| PROTO-01 | Auto-attack system | Done |
| PROTO-02 | Enemy damage and death | Done |
| PROTO-03 | Enemy spawning and respawning | Done |
| PROTO-04 | HUD overlay (HP, level, floor) | Done |
| PROTO-05 | Player death and restart | Done |
| PROTO-06 | GameState autoload and signals | Done |

---

## Phase 1 -- Complete Systems

| ID | Title | Status | Notes |
|----|-------|--------|-------|
| SYS-01 | Character class selection (Warrior/Ranger/Mage) | Done | ClassSelect screen with stat cards, skill previews, keyboard/mouse nav. Warrior=melee slash, Ranger=arrow projectile, Mage=magic bolt+staff fallback. |
| SYS-02 | Skill definitions + use-based leveling | Done | Full skill tree: 80+ skills across 3 classes, SkillDatabase, SkillTracker, use-based XP + skill points, passive bonuses, SkillTreeDialog UI in pause menu. |
| SYS-03 | Death penalty flow (XP loss, item loss, buyout) | Done | Multi-step flow: XP loss, item loss chance, gold buyout option, Sacrificial Idol prevention. |
| SYS-04 | Quest system (Adventure Guild radiant quests) | Done | QuestTracker with Kill/ClearFloor/DepthPush types, 3 active quests, scaling rewards, QuestPanel UI at Guild Master NPC, save/load. |
| SYS-05 | Level Teleporter NPC | Done | TeleportDialog with floor selection, zone labels, wired to Teleporter NPC service button. |
| SYS-06 | Blacksmith crafting UI | Done | BlacksmithWindow with Craft/Recycle tabs, AffixDatabase (28 affixes, tiers 1-4), deterministic affix application, gear recycling for gold. |
| SYS-07 | Bank UI | Done | BankWindow with deposit/withdraw, 50 start slots, expansion purchasing (500*N^2), persistent save/load. |
| SYS-08 | Items & affix system | Done | AffixDef/AffixDatabase, CraftableItem model, Crafting logic (max 3 prefix + 3 suffix), BaseQuality enum. Unique items catalog pending. |
| SYS-09 | Achievement system (Dungeon Ledger) | Done | AchievementTracker with 30 achievements across 5 categories, counter-based tracking, gold/title rewards, DungeonLedger UI in pause menu. |
| SYS-10 | Monster families (zone-exclusive creature sets) | Done | Constants.Zones with 10-floor zone system, zone-gated species spawning. Zone 1: Skeleton+Bat, Zone 2: Goblin+Wolf, Zone 3: Orc+Spider, Zone 4: DarkMage mix, Zone 5+: all. |

---

## Systems Built Beyond Original Tracker

These systems were implemented during visual-first development but were not part of the original ticket plan.

### Game Flow
| System | Scripts | Status |
|--------|---------|--------|
| Splash screen | `SplashScreen.cs` | Done -- title, character card for saved game, New Game/Tutorial/Settings/Exit |
| Class selection | `ClassSelect.cs` | Done -- 3 class cards, Up/Down zone nav (cards→confirm→back), keyboard+mouse |
| Town hub | `Town.cs` | Done -- 16x12 tile room, 5 NPCs, dungeon entrance |
| World swapping | `Main.cs` | Done -- `LoadTown()`/`LoadDungeon()` swap world scenes dynamically |
| Screen transitions | `ScreenTransition.cs` | Done -- fade-to-black with message, used for floor descent and town/dungeon travel |

### NPC & Shop
| System | Scripts | Status |
|--------|---------|--------|
| NPC interaction | `Npc.cs`, `NpcPanel.cs` | Done -- proximity detect, "[S] Interact" prompt, service menu with greeting |
| Shop system | `ShopWindow.cs`, `ItemDatabase.cs`, `Inventory.cs`, `Item.cs` | Done -- buy/sell tabs, gold tracking, item descriptions, 8 starter items |
| Dialogue system | `DialogueBox.cs` | Done -- typewriter effect, portraits, advance with S/Space/Enter |
| Toast notifications | `Toast.cs` | Done -- stacking bottom-center toasts, auto-dismiss, info/success/warning/error |

### Dungeon & Combat
| System | Scripts | Status |
|--------|---------|--------|
| Proc-gen floors | `Dungeon.cs` | Done -- random room size (18-30), 4 floor tile variations, wall border |
| Floor scaling | `Constants.FloorScaling` | Done -- room growth per 5 floors, enemy level range per floor |
| Spawn safety | `Dungeon.SpawnInitialEnemies()` | Done -- guaranteed 10 enemies per floor, fallback to any floor tile, floor wipe requires 10+ kills |
| Ascend dialog | `AscendDialog.cs` | Done -- stairs-up triggers: return to town, go up 1 floor, select specific floor |
| Unified combat | `AttackConfig.cs`, `ClassAttacks.cs` | Done -- data-driven melee/projectile, Mage auto=staff melee (magic bolt via hotbar) |
| Projectile system | `Projectile.cs` | Done -- 8 projectile sprites (arrow, magic bolt, fireball, frost bolt, lightning, stone spike, energy blast, shadow bolt) |
| Directional sprites | `DirectionalSprite.cs` | Done -- 8-direction rotation loading and velocity-based switching |
| Flash effects | `FlashFx.cs` | Done -- damage, poison, curse, boost, shield, freeze, heal, crazed flash types |
| Floating text | `FloatingText.cs` | Done -- combat damage, heals, XP, level-up numbers that rise and fade |
| Level-gap enemy colors | `Enemy.cs` | Done -- 8-anchor gradient (grey to red) based on enemy-vs-player level gap |

### UI & Debug
| System | Scripts | Status |
|--------|---------|--------|
| Debug panel | `DebugPanel.cs` | Done -- F3 toggle, shows HP/level/XP/floor/damage/enemies/kills/session time |
| Debug console | `DebugConsole.cs` | Done -- F4 toggle, god mode, XP/gold/level cheats, teleport, kill all, give items, perf metrics |
| Pause menu | `PauseMenu.cs` | Done -- Esc toggle, Resume/Backpack/Stats/Skills/Ledger/Tutorial/Settings/Main Menu/Quit |
| HUD | `Hud.cs` | Done -- Diablo-style HP/MP orbs (bottom-left/right), XP bar below skill bar, stats panel top-left |
| Skill bar HUD | `SkillBarHud.cs` | Done -- 4 skill slots (Q+W/Q+S/E+W/E+S), controller-aware labels, cooldown overlays |
| XP bar | `XpBar.cs` | Done -- progress bar below skill bar, level-up gold glow, XP loss red flash |
| HP/MP orbs | `OrbDisplay.cs` | Done -- PixelLab glass sphere sprites, colored fill rises/falls with ratio |
| Backpack window | `BackpackWindow.cs` | Done -- 5-column slot grid (64x64), action menu (Use/Drop), item detail on focus |
| Action menu | `ActionMenu.cs` | Done -- FF-style popup for item/skill context actions, keyboard nav |
| Tutorial | `TutorialPanel.cs` | Done -- 4 tabbed sections (Movement/Combat/Menus/Town), static reference |
| Settings panel | `SettingsPanel.cs` | Done -- 4 tabbed categories (Gameplay/Display/Audio/Controls), saved to disk |
| Character card | `CharacterCard.cs` | Done -- reusable PanelContainer with sprite/stats/level, used on title screen |
| Death screen | `DeathScreen.cs` | Done -- R to restart (loads town), Esc to quit, button variants |

### Save/Load & Persistence
| System | Scripts | Status |
|--------|---------|--------|
| Save/load system | `SaveData`, `SaveSystem`, `SaveManager` | Done -- auto-save on transitions, Continue from title screen |

### Stats & Progression
| System | Scripts | Status |
|--------|---------|--------|
| Stat system | `Constants.cs` (stat formulas) | Done -- STR/DEX/STA/INT with diminishing returns, class bonuses per level, free points |
| Stat allocation UI | Pause menu dialog | Done -- allocate free stat points from pause menu |
| HP regen | STA stat | Done -- passive HP regen derived from STA |
| Death penalty flow | Multi-step in `Player.cs` | Done -- XP loss, item loss, gold buyout, Sacrificial Idol |

### Loot & Economy
| System | Scripts | Status |
|--------|---------|--------|
| Loot drops | Enemy death drops | Done -- gold + item chance per enemy kill |
| Floor wipe mechanic | `Dungeon.cs` | Done -- bonus rewards when all enemies killed on a floor |
| Item system | `ItemDef`, `ItemDatabase`, `Inventory` | Done -- full item definitions with stats, descriptions, rarity |

### Navigation & Combat
| System | Scripts | Status |
|--------|---------|--------|
| Stairs compass | `StairsCompass.cs` | Done -- gold/green arrows on screen edge, auto-hide when visible |
| Hitscan projectile system | `Projectile.cs` | Done -- instant damage + cosmetic tracer, replaced physics-based collision |
| Targeting system | `AttackConfig.cs`, `ClassAttacks.cs` | Done -- 8 targeting modes + 3 projectile behaviors |
| Per-species collision | `SpeciesConfig`, `SpeciesDatabase` | Done -- unique hitbox per enemy species |
| 7 enemy species | `Enemy.cs` | Done -- Skeleton, Goblin, Bat, Wolf, Orc, Dark Mage, Spider |
| Stairs exclusion zone | `Dungeon.cs` | Done -- enemies can't spawn within 150px of staircases |

### Architecture
| System | Scripts | Status |
|--------|---------|--------|
| Constants architecture | `Constants.cs` | Done -- all magic numbers centralized: PlayerStats, EnemyStats, Spawning, FloorScaling, Leveling, Tiles, Effects, Assets, Layers, Groups, InputActions, ClassCombat, Town, Sprite |
| Strings architecture | `Strings.cs` | Done -- all player-facing strings centralized for future i18n |
| Global theme | `GlobalTheme.cs`, `UiTheme.cs` | Done -- blue buttons, gold headings, semantic color system, slot styles |
| Game settings | `GameSettings.cs` | Done -- 20+ settings across Gameplay/Display/Audio/Controls, persisted to JSON |
| GC during loading | `ScreenTransition.cs` | Done -- forced GC during loading screen transitions |
| Mana system | `GameState.cs` | Done -- Mana/MaxMana with signals, class base pools, INT regen, save/load |
| Skill execution | `SkillDef.cs`, `SkillBar.cs`, `Player.ExecuteSkill()` | Done -- 80+ skills with ManaCost/Cooldown/AttackConfig, hotbar casting |
| Reusable UI components | `GameWindow.cs`, `TabBar.cs`, `ScrollList.cs`, `ContentSection.cs` | Done -- base window, tab system, scroll nav, content helpers |
| Window stack | `WindowStack.cs`, `KeyboardNav.BlockIfNotTopmost()` | Done -- central input routing, eliminates bleed-through |

---

## P2 -- Endgame & Polish

| ID | Title | Status | Notes |
|----|-------|--------|-------|
| END-01 | Dungeon Pacts (voluntary difficulty modifiers) | Done | `DungeonPacts.cs` — 10 pacts, heat-based rewards, enemy stat multipliers, save/load |
| END-02 | Magicule Attunement (post-cap passive tree) | Done | `MagiculeAttunement.cs` — 40-node tree, 3 rings, 4 branches, keystones, floor tracking |
| END-03 | Dungeon Intelligence (adaptive AI Director) | Done | `DungeonIntelligence.cs` — 4 metrics, pressure score, spawn/aggro/elite modifiers |
| END-04 | Zone Saturation (per-zone difficulty dial) | Done | `ZoneSaturation.cs` — per-zone saturation, decay, stat/reward multipliers |
| END-05 | Depth gear tiers (new rarity at floor 50/100/150) | Done | `DepthGearTier.cs` — BaseQuality extended to 6 tiers, quality roll with floor shift |
| UI-01 | Pause menu tabbed redesign | Done | 8 tabs: Inventory, Equip, Skills, Abilities*, Quests, Ledger, Stats, System. Q/E cycles tabs. PauseMenu rewritten as programmatic `GameWindow` + `GameTabPanel`. Equipment tab wired to real EquipmentSet in SYS-11. |
| UI-02 | Load Game screen + 3-slot save system | Done | Multi-slot `SaveManager` (3 slots at `user://saves/save_{0..2}.json`), injectable `ISaveStorage`, `GameState.CurrentSaveSlot`. Splash screen has a "Continue" button (above "New Game", disabled when no saves exist) that opens `LoadGameScreen`. Three cards (populated `CharacterCard` or "Empty Slot" panel); populated cards overlay a red X button wired to `DeleteConfirmDialog`. New Game with all 3 slots full → Toast error per spec. Keyboard nav: Left/Right cycle cards, Down → Load button, further Down → Back button, Delete key triggers slot-delete on focused card. 446 unit tests green. Follow-up: add `LoadGameTests` GoDotTest suite once TEST-09 cross-suite isolation lands. |
| ART-01 | Splash screen background image | Done | `assets/ui/splash_background.png` (1920x1080, ~456KB). PixelLab generated 4 hero sprites (archway, rubble, pillar, rune-glyph — rune unused as literal 'T' letter was off-brief; replaced with procedural radial glow) composited via Pillow with vertical gradient, tiled dungeon floor texture, interior-masked warm glow through arch, rim light, bloom spill, atmospheric fog, particles, and vignette. Style matches dungeon-tileset palette. Not yet wired into `SplashScreen.cs` — awaiting visual review. |
| SYS-11 | Equipment system (10-slot, 19 equippable) | Done | `EquipmentSet.cs` (pure logic, 10 slots + 10 ring sub-slots, HasQuiver check, class-affinity 1.25× bonus, capture/restore). `EquipSlot` enum + `Slot`/`ClassAffinity` fields on `ItemDef`. `GameState.Equipment` with class-based starting gear in `Reset()`. `SavedEquipment` + full save/load round-trip. Equipment tab UI in `PauseMenu` with slot rows, equip-picker action menu, and live bonus summary. `Player.GetEffectivePrimary()` swaps Ranger bow→melee bash when no quiver equipped (`ClassAttacks.RangerBowBash`). 433 unit + 11 integration tests green, 24 new `EquipmentSetTests`. Placeholder items in `ItemDatabase.cs` will be replaced by ITEM-01 catalog wiring. |
| SYS-12 | Bank & Backpack Redesign (Guild window) | Done | Milestones 1 (spec lock) + 2a–2g + equipment-on-death closure: model rewrite (Inventory/Bank `long` gold, one-type-per-slot, Lock flag, Transfer/Drop), reusable `SlotGrid` + `NumberFormat`, new `GuildWindow` (3 tabs), refreshed `BackpackWindow` with Drop/Lock, 5-option sacrifice death dialog, Guild Maid with dedicated ART-02 sprite. Equipment-on-death wired: `DeathScreen` calls `EquipmentSet.DestroyRandomEquipped()` on Save Backpack / Accept Fate / Quit paths; loss surfaced via toast. Buyout costs (from `DeathPenalty.cs`): equipment `deepestFloor × 25`, backpack `deepestFloor × 60`, both = sum. Sacrificial Idol = free Save Both. 433 unit + 11 integration tests green. |
| ART-02 | Guild Maid NPC sprite | Done | `assets/characters/npcs/guild_maid/` — 8-directional, 92×92, female maid with glasses, dark blue dress, white apron, ledger. Wired into `Town.cs` NPC roster. |
| POL-01 | Audio system (SFX + music + ambient) | Research locked | Reopened 2026-04-20 by PO for OGA research. Research + license policy in [docs/assets/audio-pipeline.md](assets/audio-pipeline.md): §1 license discipline (CC0 / OGA-BY preferred, CC-BY DRM-review only, CC-BY-SA / CC-BY-NC / GPL rejected), §2 directory contract, §3 music inventory (9 tracks: menu / town / dungeon low-deep-infinite / boss / two stingers), §4 SFX inventory (46 files across UI / combat / character / enemy / environment / economy / notification), §5 curated OGA pack shortlist, §6 wiring plan (MusicManager + SfxManager autoloads + bus architecture + Settings audio tab), §8 acceptance criteria. Impl deferred until PO approves the shortlist and opens a dedicated POL-01-IMPL ticket. |
| POL-02 | Real sprite animations (AnimatedSprite2D) | To Do | 8-directional static rotations done; animation frames not yet |
| POL-03 | Zone visual themes (per-zone floor/wall textures) | Done | `Constants.Assets.GetZoneTheme()` — 5 themes cycling for zone 6+ |
| POL-04 | Shader effects (hit flash, outline, glow) | Partial | FlashFx uses Modulate tinting; no custom shaders yet |
| SYS-13 | Dungeon Regurgitation | Draft-spec | P2 | Per-save-slot graveyard pool: items destroyed at death can re-surface as monster drops or chest contents at or below the floor they were lost on. Brutal-low rate (0.5% mob / 3% chest), celebrated "overleveled trash drop" comedy. Spec: `docs/systems/dungeon-regurgitation.md`. |

---

## Skills & Abilities Spec Updates

| ID | Title | Status | Notes |
|----|-------|--------|-------|
| SPEC-13a | Rewrite skills.md → Skills & Abilities spec | Done | Full rewrite: dual-system (Skills + Abilities), 3 class trees, SP/AP split, reactive/pull architecture |
| SPEC-13b | Update magic.md (Elemental/Aether/Attunement) | Done | Mage: Arcane→Elemental, added Aether, Conduit→Attunement. Ranger: Arms→Weaponry, Instinct→Survival. Warrior: Inner→Discipline, Outer→Intimidation. Added Armor innate. |
| SPEC-13c | Update classes.md (new mastery structure) | Done | Updated all 3 class summaries, Innate 3→4, SP/AP terminology |
| SPEC-13d | Update pause-menu-tabs.md (7→8 tabs) | Done | Added class-specific Abilities tab (Warrior Arts / Ranger Crafts / Arcane Spells) |
| SPEC-13e | Update combat.md (ability activation flow) | Done | Skill Hotbar→Ability Hotbar, dual XP tracking, updated flows/combat.md too |
| SPEC-13f | Update hud.md (cooldowns, status effects) | Done | Ability cooldown overlay, Armor status effect, skill bar→ability bar |
| SPEC-13g | Review controls.md (terminology pass) | Done | Skills→abilities for hotbar refs, updated tab list |
| SPEC-13i | Create point-economy.md (SP/AP rates) | Done | New doc: SP 2/level, AP 3/level + milestones + combat + use-based. Updated leveling.md |
| SPEC-13k | Archive working doc + cross-reference sweep | Done | SKILLS_AND_ABILITIES_SYSTEMS.md archived, stale terminology fixed in progression.md, items.md |

---

## Testing Infrastructure

| ID | Title | Status | Priority | Notes |
|----|-------|--------|----------|-------|
| TEST-01 | ISaveStorage interface + file I/O abstraction | Done | `ISaveStorage` interface with `GodotFileSaveStorage` (prod) and `FakeSaveStorage` (tests) implementations. `SaveManager.Storage` is injectable for testing. `SaveDataJson` helper extracted from `SaveSystem` so the pure JSON codec is unit-testable without pulling the Godot-coupled `GameState` autoload into the test project. |
| TEST-02 | Save/load round-trip unit tests | Done | 13 `SaveSystemTests` covering `FakeSaveStorage` semantics, `SaveData` JSON round-trip for all populated fields including new `SavedEquipment`, item stacks with `long` counts, corrupt-JSON handling, and multi-slot independence. 446 unit tests green. |
| TEST-03 | Seed file persistence tests | To Do | P3 | When game seeds are persisted to disk for replay/sharing, add tests that write seed → read seed → generate same floor. Uses same `ISaveStorage` abstraction from TEST-01. |
| TEST-04 | E2E scene-runner tests (GdUnit4) | To Do | P2 | Wire up scaffolded `tests/e2e/` tests to actually run scene transitions via GdUnit4 `ISceneRunner`. Requires Godot binary in CI. |
| TEST-05 | CI screenshot capture on E2E failure | To Do | P3 | `GetViewport().GetTexture().GetImage().SavePng()` in GdUnit4 tests, uploaded as CI artifacts on failure. |
| TEST-06 | Integrate GodotTestDriver (Chickensoft) | Done | P1 | `Chickensoft.GodotTestDriver` v3.1.66 wraps keyboard/action input simulation. Used by `InputHelper`. |
| TEST-07 | Create game-specific test drivers | To Do | P2 | Optional convenience — write typed drivers (PauseMenuDriver, ShopDriver, NpcDriver) that wrap keyboard flows. Current `InputHelper` + `UiHelper` already cover the bases. |
| TEST-08 | GoDotTest + keyboard-nav test suite | Done | P1 | Added `Chickensoft.GoDotTest` v2.0.28. `Main.cs` boots tests via `--run-tests`. Test suites in `scripts/testing/tests/`: Splash, ClassSelect, Town, PauseMenu, Npc, Death, Transition, DeathCinematic — all use keyboard-only input, all verify specs in `docs/flows/*.md`. Run via `make test-ui`. |
| TEST-09 | Cross-suite state isolation | Done (helper shipped; per-suite adoption gradual) | P1 | `GameTestBase.ResetToFreshSplash()` helper added — each suite can call it in `[Setup]` to guarantee a fresh GameState + splash screen. Resets `GameState.Instance`, reloads the current scene (re-runs Main._Ready), awaits splash re-appearance. Existing suites will adopt gradually as they start failing; new suites should call it up front. |
| TEST-10 | CI integration for make test-ui | Done | P2 | New `ui-tests` job in `.github/workflows/ci.yml`. Runs on every PR and push (not main-push-only, unlike the existing e2e-tests job) so keyboard-nav regressions fail CI at PR time. Uses `chickensoft-games/setup-godot@v2` for Godot 4.3. Greps the output for `✗` failure marker; uploads log as artifact on every run. |
| TEST-11 | CI workflow failing at parse — no jobs dispatch | Fix in flight (PR #10) | P1 | Root cause: line 95 used `name: Coverage Gate (${{ env.COVERAGE_THRESHOLD }}% min)` — the `env` context is not allowed in job `name:` (only `github / inputs / matrix / needs / strategy / vars` are). GitHub silently rejects the workflow at preflight, no jobs dispatch, and `workflowName` falls back to the file path. Caught with `actionlint` (Go binary, downloadable from rhysd/actionlint releases — recommend wiring into the dev tooling story). Fix: dropped the threshold value from the job display name (now just "Coverage Gate"); `env.COVERAGE_THRESHOLD` continues to drive the actual `run:` step gate-check, so the value stays single-sourced via the env block and the display name can never drift from it. Branch `fix/test-11-ci-workflow`. Mark Done after PR #10 merges. |
| TEST-12 | Test inventory doc + single-test runners | Partial (inventory + unit/integration/e2e single-test done; GoDotTest method-level + screenshot baselines pending) | P1 | Opened 2026-04-20 after PO flagged "I need to do visual tests on hundreds of screens" + "give me a tool to go at each screen, flow, and every mechanic one-by-one." Part A (this PR): [docs/testing/test-inventory.md](testing/test-inventory.md) enumerates all 58 suites / ~663 tests categorized by layer (xUnit unit / integration / GoDotTest windowed / GdUnit4 e2e) with spec back-pointers. Makefile gains `make test-unit-one TEST=X`, `test-integration-one TEST=X`, `test-e2e-one TEST=X` (dotnet-test filter, exact-method), and `test-ui-one SUITE=X` (windowed, suite-level filter via Chickensoft.GoDotTest's `--run-tests=` flag). Part B (follow-up): (1) wire method-level filter into `Main.RunTests` so `test-ui-one SUITE=X TEST=Y` actually filters to one method, not the whole suite; (2) bring `ScreenshotHelper` online per Layer-3 suite + commit baseline PNGs under `docs/evidence/screenshots/<suite>/<test>.png`; (3) fill Layer-3 gap list (Blacksmith/Backpack/Stats-tab/etc); (4) auto-regen inventory in CI so new-suite-without-doc-update fails review. |

---

## Content Catalogs

| ID | Title | Status | Priority | Notes |
|----|-------|--------|----------|-------|
| ITEM-01 | Base item catalog spec + ItemDatabase expansion | Done | Full 259-item base catalog implemented in `ItemDatabase.cs` per `docs/inventory/item-catalog.md`: 75 armor + 40 main-hand + 30 off-hand + 9 quivers + 15 neck + 40 ring + 28 consumables + 22 materials. Class-voice naming (Warrior boastful, Ranger dry, Mage verbose). Unified metal ladder (Iron/Steel/Mithril/Orichalcum/Dragonite). `ItemDef` gained `Tier` field (1..5 for gear, 0 for untiered). Starting-gear `GameState.EquipStartingGear` now pulls tier-1 catalog items. Legacy IDs (`sword_iron`, `potion_hp_small`, `idol_sacrificial`, etc.) fully migrated across tests + sandboxes. 446 unit + 11 integration tests green. Combat-relevant ring focuses (crit/haste/dodge/block) ship as stat-zero — will be activated by COMBAT-01. |
| ITEM-02a | Copilot nitpicks from PR #8 merge (substantive fixes shipped in triage PR #9) | To Do | P3 | Triage PR #9 fixed the substantive bugs flagged by Copilot on PR #8: floor-100 boundary aligned with code at 101+, Bat/Goblin/DarkMage thematic biases restored to spec, `Main.cs` Load-Game transition checks `LoadSlot` success before freeing UI, `LoadGameScreen` populated-slot focus routed through `card` (was a no-op on a non-focusable wrapper Control), Left/Right from buttons zone now prefers first populated slot, `GameState.Reset()` preserves `CurrentSaveSlot` (was wiping the New-Game slot reservation), `EquipmentSetTests.CaptureAndRestore_PreservesAllSlots` now uses real catalog IDs (was silently skipping). This ticket tracks the remaining nitpicks: `LoadGameScreen.RebuildSlots` re-invokes `_Ready` via `CallDeferred` (extract a shared slot-init helper); `MonsterDropTable.CandidatesForTier` rescans `ItemDatabase.All` per kill (cache per tier); `DeathScreen` constructs `new Random()` instead of `Random.Shared`; `SaveDataJson.Deserialize` logs to `Console.Error` from a Godot-agnostic codec (move logging to the Godot-facing wrapper). Defer unless touched. |
| ITEM-02b | Species-gated quiver drop channel | Superseded | — | Replaced by SPEC-LOOT-01: monsters no longer drop equipment of any kind, so the species-gated ammo channel is moot. Quivers now drop from containers like every other equipment slot. |
| SPEC-LOOT-01 | Container loot system (Jar / Crate / Chest) — equipment moves off monsters | Done (spec) | P1 | [docs/systems/loot-containers.md](systems/loot-containers.md). Equipment is a container-only drop; monsters keep gold + XP + materials. Three container sizes (Jar / Crate / Chest), all interact-only ([S]), single-use. Spawn counts scale `+1/10 floors` (generous). Slot-uniqueness per container (no 2 rings). Jar pool = {Ring, Neck, Consumable}; Crate + Chest = full pool. Crate "bulk" = 2–4 mats per crate. Sig-mat zone-tilted toward the floor's species. Monster-drops.md updated: equipment channel + Slot Rolling Rule + Equipment Drop Rates + Quality Distribution sections removed; boss drops now spawn a bonus container instead of guaranteed equipment. |
| LOOT-01 | Implement Container.cs + ContainerLootTable.cs + Dungeon spawn integration | Done | P1 | Per SPEC-LOOT-01. New `scripts/logic/ContainerLootTable.cs` (pure logic) with per-type loot tables, floor-linear gold scaling (×(1+floor×0.05)), slot-uniqueness rule, zone-tilted signature materials, spawn-count scaling `+1/10 floors`. New `scripts/Container.cs` — StaticBody2D + Area2D interact + placeholder ColorRect visuals (ART-15 replaces), single-use open + "looted" modulate. `Dungeon.cs` wires `SpawnContainers()` after `SpawnInitialEnemies()` at both initial load + floor change; clears containers alongside enemies on floor transition. `Constants.Groups.Containers` group added. `MonsterDropTable.RollEquipment` + `CandidatesForTier` removed; `Enemy.cs` strips the equipment-drop call; `MonsterDropTableTests` loses 3 `RollEquipment_*` tests. New `ContainerLootTableTests` (19 tests) covers spawn-count scaling, gold range + scaling, generic material tier/count, signature zone-tilt, jar small-thing pool, chest/crate slot-uniqueness, end-to-end Roll. |
| ART-15 | Container sprites (jar, crate, chest) | To Do | P2 | Per SPEC-LOOT-01. **Two states each — closed (intact) and open (post-loot).** Jar open = broken/shattered jar with shards. Crate open = splintered crate with lid off. Chest open = lid raised, interior visible. Optional 2–3 visual variants per type for visual variety on a single floor. The open sprite is the "already looted" marker that persists until the player leaves the floor. |
| ITEM-02 | Monster drop tables spec + wiring | Done | `MonsterDropTable.cs` implements per-species drop tables per `docs/systems/monster-drops.md`. Per-species signature material IDs + rates (Skeleton→Bone Dust 10%, Bat→Echo Shard 8%, Goblin→Goblin Tooth 10%, Wolf→Wolf Pelt 10%, Orc→Orc Tusk 10%, Spider→Chitin Fragment 8%, Dark Mage→Arcane Residue 7%). `RollEquipment(species, floor)` rolls base+floor-bonus rate then picks from catalog items at the floor's tier. `RollMaterials(species, floor)` rolls 25% generic (with thematic 60/20/20 type bias) + independent signature roll. `Enemy.cs` swapped from `LootTable.RollItemDrop` to species-aware drops. 24 new `MonsterDropTableTests` covering floor→tier mapping, signature rate sampling, thematic material bias. 470 unit + 11 integration tests green. |
| FORGE-01 | Blacksmith Forge RNG system (spec + impl) | Draft-spec | P2 | Spec drafted: [docs/systems/blacksmith-forge-rng.md](systems/blacksmith-forge-rng.md). Player-owned base + large material investment rolls one unique from a per-tier pool (28 seed uniques across T1..T5). ONE-SHOT (base consumed), named templates (hand-designed affix packages), REPLACE base affixes. Gated by base tier only (depth gate is implicit — you can't drop T5 bases below floor 51). Forge is a 3rd Blacksmith tab. Uniques live in `UniqueItemDatabase`, not `ItemDatabase`. Awaiting user review. |
| COMBAT-01 | Equipment stats into combat formulas | Ready-for-impl | P1 | Spec locked: [docs/systems/combat-equipment-integration.md](systems/combat-equipment-integration.md). Overlay model (equipment STR stacks onto allocated STR before DR curve), cache-on-equip with invalidation hooks on every mutator, new `EquipmentCombatStats` struct carries raw crit/haste/dodge/block % from ring catalog. **Split chance-vs-power soft-cap** model (no flat 60% walls): effective = `raw * (60 / (raw + 60))`, overflow converts — Crit overflow × 2 → crit-damage bonus (no hard cap, base 1.5× + bonus), Haste overflow × 0.5 → Flurry free-extra-swing chance (**hard cap 40%**, rationale: preserves enemy telegraph timing), Dodge overflow → Phase i-frame duration in ms (**hard cap 500 ms**, auto-consumes next hit or expires), Block overflow × 0.5 → reduction % above 50% baseline (**hard cap 80% total**). Resolution order: Phase → Dodge → Block in `GameState.TakeDamage`. Single `RecomputeDerivedStats()` helper unifies the four current MaxHp recompute sites. Same DR curve shape as stats.md (K=60 instead of K=100) for intuition reuse. 10× T5 Precision worked example in spec: 37.5% effective crit + 2.75× crit multiplier, 1.66× per-swing expected damage — proves the "infinite part" works at arbitrary depth. |
| COMBAT-02 | Weapon archetype cross-class attack dispatch | Draft-spec | P2 | Spec drafted: [docs/systems/weapon-archetype-dispatch.md](systems/weapon-archetype-dispatch.md). Dynamic dispatch on every attack (not cached); new `WeaponArchetype` enum on `ItemDef`, new `AttackScalingStat` enum on `AttackConfig`. Archetype dictates both attack mechanic AND scaling stat (bow = DEX, staff = INT, sword = STR). Adds `StatBlock.RangedDamageMultiplier` (`effective_dex * 0.6%`) — requires stats.md edit. Bow-bash fallback generalized to all classes (not just Ranger). Per-archetype tuning variants flagged as future COMBAT-03. Awaiting user review. |
| ART-03 | Catalog armor sprites (Head/Body/Arms/Legs/Feet × 5 tiers × 3 classes) | To Do | P2 | 75 armor sprites total. Class-voice visual differentiation (Warrior plate vs Ranger leather vs Mage robes). PixelLab batch generation per art-lead. Blocks ITEM-01 visual polish but not core ITEM-01 data wiring. |
| ART-04 | Catalog weapon sprites (Main Hand × 40 + Off Hand × 30) | To Do | P2 | 70 weapon/offhand sprites. Swords/axes/hammers (Warrior), shortbow/longbow/crossbow (Ranger), staves/wands (Mage), shields/defensive-melee/spellbooks. |
| ART-05 | Catalog quiver sprites (9 imbue types) | To Do | P2 | 9 quiver sprites (Basic/Hot/Cold/Heavy/Nasty/Zap/Quiet/Sharp/Bright). Element-coded visuals. |
| ART-06 | Catalog accessory sprites (Neck × 15 + Ring × 40) | To Do | P3 | 55 small-icon sprites. Metal ladder visual cues (Iron/Steel/Mithril/Orichalcum/Dragonite). Likely smallest art lift — icon-only, no directional rotations. |
| ART-07a | Skill / Ability icons — MVP hotbar + mastery set | To Do | P2 | Diablo / Hellfire spell-icon style reference (square tile background, white/grey pictograph, occasional red accent; **inspiration only, no direct port**). MVP scope: 27 mastery icons (section headers for all 3 classes) + 12 common hotbar abilities (~40 icons). Shared stone-bg template + per-icon pictograph overlay. 64×64 PNG. Unblocks Skills/Abilities tabs feeling visually complete for the default-play scenario. |
| ART-07b | Skill / Ability icons — long-tail roster | To Do | P3 | Remaining ~90 ability icons across all 3 classes to fill out the full 103-ability SkillAbilityDatabase. Same template + pictograph pipeline established in ART-07a. Polish wave after core game loop feels complete. |
| ART-08 | Class portraits (Warrior / Ranger / Mage) | To Do | P2 | 3 large-format portraits (~256×384) for splash screen, Class Select cards, and Character Card on Load Game. Currently using small rotation sprites upscaled — looks low-fi. Higher-res portraits would elevate every character-card surface in the game. |
| ART-09 | NPC dialogue portraits | To Do | P3 | Per-NPC portrait images for `DialogueBox` (portrait slot is already reserved but unpopulated). 5 NPCs: Guild Master, Blacksmith, Guild Maid, Teleporter, Village Chief. One neutral + one expression variant per NPC = ~10 portraits. |
| ART-10 | Boss sprites (zone bosses) | To Do | P3 | `monster-drops.md` references boss first-kill drops but no distinct boss art exists yet. Target: 1 boss per 10-floor zone = ~5-10 boss sprites, visually distinct from species-tier mobs (larger, unique silhouette per boss). |
| ART-11 | Dungeon / town decoration sweep | To Do | P3 | Audit-then-fill of environmental props: torches, chests, doors, stair ornamentation, town signage, lanterns, the dungeon-entrance marker in Town, barrels, crates. Scope TBD after audit; likely ~20-30 sprites. |
| ART-12 | Biome tile-variant expansion + pattern (**ISOMETRIC**) | Blocked on ISO-01 | P2 | Live rendering is **already isometric** — this tile pass starts after ISO-01 completes the remaining gaps (diamond wall collision, root Y-sort, sprite anchors, camera bounds, occlusion shader). **Pattern per biome** (dungeon / dungeon_dark / cathedral / nether / sky_temple / volcano / water / town): 4-6 floor diamond variants, wall pieces per orientation (NW / NE / SW / SE faces + corners + T-junctions + cross-junctions), door/opening variants, stairs. **~30 iso-tiles × 8 biomes = ~240 tiles**. Reference: Diablo 1 Catacombs tile grid (inspiration only, no 1:1 port). Each biome follows the same slot pattern. |
| ART-13 | Environmental objects per biome (**ISOMETRIC**) | Blocked on ISO-01 | P2 | Iso object sprites authored for the live iso renderer: pillar, rubble pile, biome-themed statue, biome-themed altar/well/pit, 1-2 thematic props (chains + bones + hanging cage for dungeon; candelabra + pews + cross for cathedral; lava geyser + obsidian slab for volcano; crystal cluster + cloud pedestal for sky_temple). **~8 objects × 8 biomes = ~64 iso sprites**. Includes multi-tile objects (e.g., 2×2 altar) for visual scale variance. |
| ART-14 | Replace Bat / Spider / Wolf with true beast/insect sprites (not bipedal) | To Do | P2 | Current Bat, Spider, and Wolf enemy sprites are bipedal humanoid silhouettes (placeholder pipeline output) — they should be true quadrupeds / true winged / true arachnid. Bat = airborne small flyer (4-direction or single-frame hover). Spider = 8-legged arachnid, low silhouette, true top-down/iso footprint. Wolf = 4-legged canid, mid-size. 8-direction sprites per species to match existing `DirectionalSprite.cs` pipeline. May require `SpeciesConfig` hitbox revision (e.g., Bat smaller/airborne, Spider wider/lower). Coordinate timing with ISO-01 — sprites should be authored for the live iso render mode. |
| SPEC-ISO-01 | Isometric rendering — completion + content contract spec | Draft (PO review) | P1 | **Reframed (was "Major architectural pivot from 32×32 top-down").** Audit of current code shows the engine layer is **already isometric**: `TileShape = Isometric`, `TileSize = (64,32)`, `TextureRegionSize = (64,64)` for both floors and walls, single shared TileMapLayer per scene, WASD = visual cardinal movement via `Velocity = inputDir.Normalized() * MoveSpeed; MoveAndSlide();` — all already shipping. Spec [docs/systems/iso-rendering.md](systems/iso-rendering.md) covers the remaining gaps: (1) wall collision polygon (rect → diamond), (2) scene-root Y-sort wiring (so TileMapLayer + Entities interleave per-row instead of by scene-tree order), (3) bottom-center sprite anchor convention (replaces empirical `+Vector2(0,40)` spawn offset in Dungeon.cs), (4) iso camera-bounds, (5) wall-occlusion shader (player behind tall walls), (6) per-biome content directory contract for ART-12/13. Locks Godot 4 movement idiom (`Velocity` in px/s, `MoveAndSlide()`, no `global_position` writes in movement path). ISO-01 sub-phases (a–f) sequenced as small independent PRs. Touch surface enumerated for code + scenes + downstream docs. Awaiting PO review before ISO-01 unblocks. |
| ISO-01 | Isometric rendering — completion + occlusion impl | Blocked on SPEC-ISO-01 | P1 | Implement the spec. **Not a rewrite** — completes the gaps the spec audit identified. Diamond wall collision polygon in `Constants.cs`; `y_sort_enabled = true` on `Dungeon` / `Town` scene roots in their .tscn files; bottom-center sprite anchor on `player.tscn` / `enemy.tscn` / NPC sprites (which lets us remove the empirical `+ Vector2(0, 40)` spawn offset in `Dungeon.cs:39` and `:127`); iso camera-bounds in `Dungeon.cs`; new `IsoTransform.cs` helper for off-tilemap math; wall-occlusion fragment shader on the shared TileMapLayer with `is_wall` tile-data flag. **Movement code is unchanged** — already correct Godot 4 idiom. Sub-phases (small independent PRs): ISO-01a coord helpers + tests, ISO-01b diamond wall polygon, ISO-01c root Y-sort wiring, ISO-01d sprite anchors + spawn-offset cleanup, ISO-01e camera bounds, ISO-01f wall-occlusion shader. |
| ART-SPEC-01 | PixelLab prompt template library | Done (spec) | P1 | **Rewritten 2026-04-17 for true-iso + cartoonish + IP-clean — v1 (commit `375f42e`) superseded; subsequent IP sweep at `5e9e70f`.** Locked in [docs/assets/prompt-templates.md](assets/prompt-templates.md). Pivot: engine is true 2:1 iso per SPEC-ISO-01; v1's "low top-down" preamble was wrong and produced the empirical `+Vector2(0,40)` spawn offset ISO-01d removes. Style direction: cartoonish pixel art with bold chunky silhouettes and slightly exaggerated proportions in true 2:1 dimetric — genre-generic dungeon-crawler inspiration only, zero named-IP in prompts. Seven named blocks: **CHAR-HUM-ISO** (humanoid chars / classes / NPCs / bipedal monsters, 128×128), **CHAR-MON-ISO** (non-humanoid monsters, `.quad` / `.wing` / `.arach` / `.biped-mon` sub-variants), **TILE-ISO-ATLAS** (floor diamond + wall faces + corners + junctions + stairs + doors, per-biome sub-clamps for 8 biomes, ~30 slots/biome × 8 = ~240 tiles), **OBJ-ISO** (props bottom-center anchored, 64-wide footprint with variable height), **PROJ-ISO-8DIR** (32×32 projectiles, 8-dir), **ICON-UI-64** (64×64 ability icons, stone-tile plaque frame), **PORTRAIT-NPC** (256×256 2D bust, UI-only), **PORTRAIT-CLASS** (256×384 2D hero art, UI-only). Locked perspective + canvas contract: bottom-center anchor rule with derived `Sprite2D.offset.y = -(H/2) - 16` formula, 8-direction rotation naming mapped to iso screen space, per-family canvas + view-parameter table. §11 IP Protection: six hard rules (no named-IP in prompts, cartoonish qualifier is load-bearing, no 1:1 asset replication, fresh-authored identity, palette is ours, PR audit trigger). Redraw policy: every shipped character / monster / tile / object slated for re-gen; exempt = game logo + game icon + HP orb + MP orb. Drift prevention: 6-item PR checklist (v1's 5 + iso-alignment bullet). Extension protocol: ≥3-assets + body-plan/format/perspective/canvas gate + worked-example-at-lock. Unblocks wave 2 family specs ART-SPEC-02 → ART-SPEC-09. |
| SPEC-SPECIES-01 | Monster species authoring template (design half) | Done (spec) | P1 | **Tag-team with ART-SPEC-02.** Template locked at [docs/world/species-template.md](world/species-template.md). Eight required sections per species: (1) Identity — fiction beat + ecology role + one of five locked emotional reactions (cautious-approach / burst-down-fast / kite-from-range / close-the-gap / pack-management); (2) Stats — base HP/damage/speed/XP at three sample floors (3 / 28 / 75) plus reaction-consistent TTK targets; (3) AI Pattern — one of five locked patterns (melee-chase / ranged-kite / ambush / pack / caster) with telegraph-ms / aggro / leash fields; (4) Drop-Table Hook — signature material id + 7–10% rate + thematic generic (Ore/Bone/Hide); (5) Silhouette Readability Constraint — one binding sentence + rationale (the gameplay-to-visual hook); (6) Size / Scale Rule — multiplier band (Small/Standard/Large/Boss) + hitbox radius + z-offset for flyers; (7) Color-Coding Contract — base tint surface + exempt-pixel list for species-identity pixels that must survive the level-relative modulate; (8) Art-Spec Pairing — cited ART-* ticket + two-checkbox lock stamp. Includes worked Bat example (SPEC-SPECIES-BAT-01) and an 8-row Acceptance Matrix. Locks alongside ART-SPEC-02. |
| ART-SPEC-02 | Species sprite generation spec (art half) | Done (spec) | P1 | **Locked (paired with SPEC-SPECIES-01).** [docs/assets/species-pipeline.md](assets/species-pipeline.md). Maps SPEC-SPECIES-01 fields to CHAR-MON-ISO sub-variants (.quad/.wing/.arach/.biped-mon), scale values (0.70/1.00/1.30/2.00), +28px airborne z-offset, and template animation IDs. Sub-variant trigger table per AI pattern, 8-dir carve-out for Small+pack-management airborne, palette-clamp vs exempt-pixel resolution rule. Bat worked example with full copy-paste prompt (IP-clean per ART-SPEC-01 §11). Handoff checklist unblocks ART-14 (Bat/Spider/Wolf rework) + all future species redraws. **Delete-before-regen: Bucket B in [asset-inventory.md](assets/asset-inventory.md).** |
| ASSET-INV-01 | Pre-redraw asset inventory snapshot | Done (doc) | P0 | [docs/assets/asset-inventory.md](assets/asset-inventory.md). Frozen at commit 375f42e. Labels every existing image as KEEP (logo/icon, HP/MP orbs) or REDRAW (every other sprite/tile/icon/effect). Groups redraw targets into Buckets A–K, each tied to an ART-SPEC-* ticket. Locks the delete-before-regen protocol: each redraw batch deletes its bucket in the first commit on a dedicated branch, regenerates in the second, fixes scene refs in the third. **NPC roster locked to 3** (Blacksmith / Guild Maid / Village Chief) per PO 2026-04-17; service consolidation table in Bucket C. Flags `splash_background.png` for ART-SPEC-09-time decision. Effects Bucket K deferred per PO (no ticket until Wave 2 lands). |
| NPC-ROSTER-REWIRE-01 | Consolidate NPC services to 3-NPC roster (code) | Done | P1 | Landed 2026-04-18. Town scene now spawns exactly 3 NPCs (Guild Maid, Blacksmith, Village Chief). Teleporter retired. Merged in PR #21. Follow-up tickets BLACKSMITH-MENU-IMPL-01 + GUILD-MAID-MENU-IMPL-01 landed same-day — see rows below. Bucket C art redraw (ART-SPEC-NPC-01 art-side) now unblocked. |
| BLACKSMITH-MENU-IMPL-01 | Restructure BlacksmithWindow to 4-tab layout (Forge + Craft + Recycle + Shop) | Done | P1 | Landed 2026-04-18 per SPEC-BLACKSMITH-MERGED-MENU-01. `scripts/ui/BlacksmithWindow.cs` rewritten to use `GameTabPanel` instead of the homegrown 2-tab toggle. Tabs in spec order: **Forge** (affix application — inherits prior behavior, still a stub awaiting the affix-apply dialog), **Craft** (material-to-item recipes — placeholder "coming soon" until the recipe system lands), **Recycle** (break down gear, unchanged logic), **Shop** (caravan-stocked consumables — ported from the prior Guild Store tab; uses the same `ItemDatabase` consumable filter + Buy dialog + Send-to-Bank/Backpack toggle as before). Opens on Forge by default. Q/E cycles tabs, 1-4 jumps directly. Gold footer shows backpack gold only (bank gold surfaces in the Guild tab). Follow-ups: actual affix-application dialog (Forge tab), recipe content + tab UI (Craft tab), `BlacksmithShopStock` item-database tag (Shop tab content source). None blocking for this impl. |
| GUILD-MAID-MENU-IMPL-01 | Restructure GuildWindow to 2-tab layout (Bank + Teleport) | Done | P1 | Landed 2026-04-18 per SPEC-GUILD-MAID-MERGED-MENU-01. `scripts/ui/GuildWindow.cs` refactored from the prior 3-tab (Store/Bank/Transfer) to a 2-tab (Bank/Teleport) layout. **Store tab moved OUT** to Blacksmith's new Shop tab (via BLACKSMITH-MENU-IMPL-01, same-day atomic change). **Transfer tab collapsed INTO Bank** — the Bank tab now has a top gold row (Withdraw/Deposit All), a two-column Bank↔Backpack layout (slot-click fires an action-menu with Transfer/Sell/Lock options per side), and a bottom upgrade button. **Teleport tab added** — absorbed from the prior `TeleportDialog` / retired Teleporter NPC; floor list descending from deepest visited, click to teleport with the same ScreenTransition flow as before. `NpcPanel` reverted to single "Open Guild" button for Guild Maid (Teleport is inside the window now). `TeleportDialog` left in place as dead code for the legacy Teleporter NPC dispatch path (test compat); removal is a separate cleanup ticket. `docs/ui/guild-window.md` marked partially superseded. |
| SPEC-PC-ART-01 | Player class visual identity (design half) | Done (spec) | P1 | **Paired with ART-SPEC-PC-01.** [docs/world/player-classes-art.md](world/player-classes-art.md). Locks: **single default sprite per class** (PO 2026-04-17) — Warrior plate+sword+shield, Ranger leather+shortbow+hood, Mage robes+staff+hat; equipment changes surface via inventory paperdoll UI only, never on world sprite. Silhouette differentiation on three orthogonal axes: Warrior=widest (shoulders+shield), Mage=tallest (pointed hat extends above head), Ranger=narrowest (no shoulder armor, bow held close). Readable at 64×64 thumbnail. Scale=1.00 canonical reference. PCs exempt from level-relative tint (player-blue #8ed6ff accent zones per class). Indeterminate gender, 20s–40s age range. Worked Warrior example. **Delete-before-regen: Bucket A in [asset-inventory.md](assets/asset-inventory.md).** |
| ART-SPEC-PC-01 | Player class sprite pipeline (art half) | Done (spec) | P1 | **Paired with SPEC-PC-ART-01.** [docs/assets/player-class-pipeline.md](assets/player-class-pipeline.md). Extends CHAR-HUM-ISO from ART-SPEC-01. Three complete copy-paste PixelLab prompts (Warrior / Ranger / Mage) with all slots filled, 128×128 canvas, 8-dir. Animation recipe (walking + fight-stance-idle-8-frames + per-class attack: cross-punch/lead-jab/fireball template IDs). Download layout under `assets/characters/player/{warrior,ranger,mage}/`. 64×64 silhouette-verification PR gate. Cartoonish qualifier inherited from ART-SPEC-01 preamble; zero named-IP in any prompt. **Delete-before-regen: Bucket A in [asset-inventory.md](assets/asset-inventory.md).** |
| ART-SPEC-03 | Tile pipeline — Dungeon exemplar + 7-biome substitution | Done (spec) | P1 | **Blocks ART-12.** [docs/assets/tile-pipeline.md](assets/tile-pipeline.md). Extends TILE-ISO-ATLAS from ART-SPEC-01. Full 30-slot Dungeon biome atlas: 6 floor variants + 17 wall pieces (faces + outer corners + inner corners + T-junctions + cross) + 4 stairs + 3 door arches — every iso-adjacency case covered. Each slot has a complete copy-paste `create_map_object` prompt (not `create_isometric_tile` — that tool produces wrong 3D-block style). Other 7 biomes (dungeon_dark / cathedral / nether / sky_temple / volcano / water / town) templated via palette-hook + motif-hook substitution table with worked Cathedral floor-0 example. `dungeon_dark` palette hook is a hard override of Dungeon (no moss, no torch-amber) so biomes read distinct. Flat `assets/tiles/{biome}/` layout. Zero named-IP in prompts. Seamless-tiling PR checklist. **Delete-before-regen: Bucket D in [asset-inventory.md](assets/asset-inventory.md).** |
| SPEC-NPC-ART-01 | Town NPC visual identity (design half) | Done (spec) | P1 | **Paired with ART-SPEC-NPC-01.** [docs/world/npc-art.md](world/npc-art.md). Locks 3 NPCs: **Blacksmith** (forge+craft+shop, stout figure with hammer over shoulder + leather apron + soot-streaked face + horn-scale pauldron adventuring memento), **Guild Maid** (bank+teleport, maid-uniform silhouette + ledger/keys/teleport pendant at hip), **Village Chief** (quests, elderly stooped figure + silver chain-of-office + sage-green robes + gnarled wood walking staff — fresh design, no prior sprite). Scale 0.95× (non-threatening); 4-direction + idle (static town, no walk); PCs exempt from player-blue accent to keep NPC-vs-PC silhouettes distinct. 5 hard constraints on NPC-vs-PC differentiation (no plate / no bow / no pointed hat with glowing staff / etc). Voice–visual compatibility check per NPC. **Delete-before-regen: Bucket C in [asset-inventory.md](assets/asset-inventory.md).** Blocked by `NPC-ROSTER-REWIRE-01` (code must land first). |
| ART-SPEC-NPC-01 | Town NPC sprite pipeline (art half) | Done (spec) | P1 | **Paired with SPEC-NPC-ART-01.** [docs/assets/npc-pipeline.md](assets/npc-pipeline.md). Extends CHAR-HUM-ISO. Three copy-paste PixelLab prompts (Blacksmith / Guild Maid / Village Chief) — 128×128 canvas @ 0.95× render scale, 4-direction coverage, `breathing-idle` animation only (NPCs don't walk). 12-frame total town NPC animation budget. Download layout under `assets/characters/npcs/{blacksmith,guild_maid,village_chief}/`; Bucket C sweep deletes obsolete `banker/shopkeeper/guild_master/teleporter/` dirs in the same PR. 6-up PC-vs-NPC thumbnail gate in PR checklist. **Pre-req `NPC-ROSTER-REWIRE-01` must merge first before Bucket C redraw PR.** |
| ART-SPEC-04 | Map object pipeline — per-biome environmental props (art) | Done (spec) | P1 | **Blocks ART-13.** [docs/assets/object-pipeline.md](assets/object-pipeline.md). Extends OBJ-ISO. Five object families: **Light** (torches / braziers / lanterns / candelabra), **Structural** (pillars / columns / arches / rubble), **Sacred** (altars / shrines / statues), **Hazard** (lava / poison / fire / water — redraws of 6 existing `assets/effects/*.png` files, re-categorized as bottom-center iso props), **Decor** (biome-specific thematic props). 16 Dungeon biome slots with full copy-paste prompts; 7 non-Dungeon biomes templated via palette+motif substitution (4 worked examples: Cathedral / Volcano / Sky_temple / Nether). Family-first directory layout `assets/objects/{family}/{biome?}/{name}.png`. Town-biome sacred slots reframed (altar→fountain, shrine→sign-post, gargoyle→weathervane) — static town has no religious fixtures. Hazard-effects migration: 6 Bucket K files → objects; 12 remain FX for future ART-SPEC-FX-01. Zero named-IP. |
| ART-SPEC-05 | Container pipeline (jar/crate/chest × closed/open) | Done (spec) | P1 | **Blocks ART-15 (+ LOOT-01 scene wire-up).** [docs/assets/container-pipeline.md](assets/container-pipeline.md). Extends OBJ-ISO for SPEC-LOOT-01. 3 types × 2 states × 2 variants = 12 sprites. Canvas: Jar 64×64, Crate 64×64, Chest 64×64 closed / 64×96 open (raised lid). 9 copy-paste Dungeon prompts (6 closed+open + 3 variants). Per-biome variation via runtime `Modulate` tint (no per-biome sprite variants — keeps asset count manageable). State-swap contract for Container.cs (LOOT-01): simple texture swap, bottom-center anchor stable; chest-open case documents `Sprite2D.Offset` change from `(0, -32)` closed to `(0, -48)` open to account for the taller canvas. `metadata.json` schema locked. |
| ART-SPEC-06 | Equipment catalog icon pipeline (259 inventory icons) | Done (spec) | P2 | **Blocks ART-03 / ART-04 / ART-05 / ART-06.** [docs/assets/equipment-catalog-pipeline.md](assets/equipment-catalog-pipeline.md). Introduces `ITEM-ICON-64` sibling block to ART-SPEC-01's ICON-UI-64 for detailed-item illustration (as opposed to pictograph-style skill icons). Locks the 5-metal palette ladder per-tier sub-clamps (Iron dull gray+rust / Steel polished silver+blue / Mithril pale-blue-silver shimmer / Orichalcum warm gold+bronze / Dragonite iridescent purple-green shift). 7 prompt skeletons (armor / weapons / offhands / quivers / neck / ring / consumables / materials) + 7 worked examples. Class-voice armor palette (Warrior plate / Ranger leather / Mage cloth). Tier-banded generation plan (Tier 1 across all categories first, then Tier 2-5 per category — ~50 Tier-1 sprites bootstrap inventory UI). Asset layout `assets/items/{category}/{itemId}.png` keyed to ItemDatabase. 32×32 thumbnail readability PR gate. Bucket G (empty — first regen). |
| ART-SPEC-07 | Projectile pipeline (9 sprites, 8-dir atlas) | Done (spec) | P2 | **Blocks projectile redraw.** [docs/assets/projectile-pipeline.md](assets/projectile-pipeline.md). Extends PROJ-ISO-8DIR from ART-SPEC-01. 9 projectiles (arrow / magic_arrow / magic_bolt / fireball / frost_bolt / lightning / stone_spike / energy_blast / shadow_bolt). 256×32 horizontal atlas, **east-indexed frame order** (angle 0 = east = frame 0) per `Projectile.cs:47-50` engine contract. Option C mixed generation: 5 asymmetric projectiles (arrow / magic_arrow / frost_bolt / lightning / stone_spike) generated as 8 separate directions via `create_map_object`; 4 symmetric (fireball / magic_bolt / energy_blast / shadow_bolt) generated once + programmatic rotation. Total 44 PixelLab calls vs 72 naive. Pillow compositing script template in-spec. All 9 full copy-paste prompts written (manageable count). Cartoonish qualifier (chunky flame orbs with exaggerated tongues, not photorealistic) load-bearing. Bucket H (9 files delete-before-regen). |
| ART-SPEC-08 | Ability / skill / mastery icon pipeline (~130 icons) | Done (spec) | P2 | **Blocks ART-07a (MVP) + ART-07b (long-tail).** [docs/assets/ability-icons-pipeline.md](assets/ability-icons-pipeline.md). Extends ICON-UI-64 from ART-SPEC-01. Per-category accent color convention locked (Mastery=gold, Warrior-active=red, Ranger-active=green, Mage-active per element — Fire=orange-red / Cold=blue-cyan / Earth=brown / Lightning=yellow-white / Light=gold-white / Shadow=purple+red, Passive=white, Ultimate=gold). Genre-generic pictograph vocabulary (sword / arrow / orb / shield / wall / feet / heart / leaf / eye / hand / rune-shape / element-tag). Full 27-mastery pictograph map (§5). 12 MVP ability full copy-paste prompts (4 Warrior + 4 Ranger + 4 Mage). Long-tail ~90 ability icons authorable from §6 skeleton + vocabulary without reopening spec — implementer picks best-fit pictograph from locked set at generation time. Per-class atlas compositing recipe (atlas_warrior / atlas_ranger / atlas_mage / atlas_innate, each with grid layout + atlas_manifest.json). 32×32 hotbar readability PR gate. Bucket I (~62 files delete-before-regen). |
| SPEC-BOSS-ART-01 | Boss visual identity (design half) | Done (spec) | P2 | **Paired with ART-SPEC-BOSS-01.** [docs/world/boss-art.md](world/boss-art.md). Boss = deepest denizen of its zone (ordinary species + years of magicule accumulation + unmoved). Template adapts SPEC-SPECIES-01 with boss multipliers: HP ×5–8, damage ×2–3, speed ×0.8, XP ×10. Phase-shift convention at 50% / 25% HP (visual cue + AI change). "Bigger and badder" two-cue silhouette rule: scale band Boss (1.7–2.5×) + unique silhouette cue absent from zone species. 8-boss starter roster mapped to zones 1-8: Bone Overlord / Howling Pack-Father / Chitin Matriarch / Hollow Archon / Warlord of the Fifth / The Screaming Flight / Iron-Gut Goblin King / Volcano Tyrant. Zone-1 Bone Overlord fully worked; zones 2-8 skeleton-filled (no TBDs). Zone→FORGE-01 unique-pool tier mapping locked (zones 1-4 → tiers 1-4, zones 5-8 → tier 5). Titles only per NPC-naming convention. |
| ART-SPEC-BOSS-01 | Boss sprite pipeline (art half) | Done (spec) | P2 | **Paired with SPEC-BOSS-ART-01.** [docs/assets/boss-pipeline.md](assets/boss-pipeline.md). Extends CHAR-MON-ISO from ART-SPEC-01 with boss-tier overrides: canvas 160×160, `Sprite2D.Offset.y = -96`, scale 1.7×–2.5× (default 1.8× / Final 2.2×), 8-direction mandatory (no Small-airborne carve-out), `falling-back-death` animation required. Full copy-paste prompts for all 8 zone bosses, **synced to SPEC-BOSS-ART-01 canonical roster** (initial roster drafted pre-design-lock was superseded 2026-04-18). Each prompt uses the boss's canonical base species body plan + silhouette cue + scale accent. 96×96 thumbnail distinctness PR gate (boss vs zone species). Zero named-IP. Bucket E (empty — first regen creates). |
| ART-SPEC-09 | Portrait pipeline (class + NPC, 2D UI) | Done (spec) | P2 | **Blocks ART-08 (3 class portraits) + ART-09 (6 NPC dialogue portraits).** [docs/assets/portrait-pipeline.md](assets/portrait-pipeline.md). Extends PORTRAIT-CLASS (256×384, three-quarter hero pose) + PORTRAIT-NPC (256×256, bust shot) blocks from ART-SPEC-01. **Explicit non-iso override** — portraits are 2D UI only; perspective clause from ART-SPEC-01 preamble is substituted. Cartoonish qualifier reinforced (slightly exaggerated proportions at bust scale, NOT photorealistic concept art drift). 3 class portraits + 6 NPC portraits (3 NPCs × neutral + conversational). Identity-parity rule for expression variants (two `create_map_object` calls, not animate_character). Asset layout `assets/ui/portraits/{class,npcs}/`. **`splash_background.png` decision locked: Option 3 — Remove.** Rationale: class portraits carry visual weight; separate bg risks style drift. Stubs impl ticket `SPLASH-BG-REMOVE-01` for asset removal + `SplashScreen.cs` dark-gradient compositional backdrop. Bucket J (first regen). |
| SPLASH-BG-REMOVE-01 | Remove splash_background.png + code composit backdrop | To Do | P3 | **Impl stub per ART-SPEC-09 §9.** Delete `assets/ui/splash_background.png` + `.import`. Update `SplashScreen.cs` to render dark-gradient backdrop via `ColorRect` (or shader) instead of an ImageTexture; position class portrait / CharacterCard in front. Independent of ART-08 landing order. No design work — pure asset removal + code swap. |

---

## Spec Roadmap Tickets — Phase B (Magic System Foundation)

Tracked here once spec'd. Full roadmap: [docs/spec-roadmap.md](spec-roadmap.md). Each spec is docs-only (no code edits per commit `7e59e56`); impl follow-ups get their own audit/ticket entries if needed.

| ID | Title | Status | Priority | Notes |
|----|-------|--------|----------|-------|
| SPEC-MAGICULE-DENSITY-01 | Magicule density vs floor — piecewise formula + danger threshold | Spec'd | P1 | Locked 2026-04-18. Piecewise formula in [magic.md §Density Formula](systems/magic.md#density-formula-spec-magicule-density-01): linear `density = F/100` for floors 1–100, then exponential `density = 1.0 · k^(F-100)` with `k = 1.032` above. Doubles every ~22 floors past threshold. Danger onset at floor 100 (density 1.0); effective ceiling at floors 180–200 (density ≈12–23); beyond stable reach at floor 250 (density ≈110). Closes `magic.md` Open Questions Q2 (scale formula) and Q3 (danger threshold). Cross-ref added in `world/dungeon.md` §Magicule Density Gradient. No code changes — this is the load-bearing magic number every downstream Phase B/C/D spec inherits (SPEC-INNATE-MANA-COST-01, SPEC-INNATE-STACKING-01, SPEC-MAGIC-COMBAT-FORMULA-01, `magicule-attunement.md`, `dungeon-pacts.md`, `dungeon-intelligence.md`). |
| SPEC-INNATE-MANA-COST-01 | Per-Innate mana drain at level 1 + per-level scaling curve | Spec'd | P1 | Locked 2026-04-18. Full spec in [magic.md §Drain Scaling Per Level (SPEC-INNATE-MANA-COST-01)](systems/magic.md#drain-scaling-per-level-spec-innate-mana-cost-01). **Level-1 drain rates (asymmetric by design role, anchored on Mage's 200-mana base pool with no regen and no density modifier):** Haste 13.33 mana/s → ~15s uptime (burst tool); Sense 6.67 mana/s → ~30s uptime (exploration tool); Fortify 3.33 mana/s → ~60s uptime (held stance). Armor is excluded — it has no drain. **Shared scaling curve:** `drain(L) = max(base_drain * 0.96^L, base_drain * 0.25)` — 4% compounded reduction per level, floored at 25% of base at level ~35. **Explicit non-coupling:** drain is NOT modified by floor density (design decision: Innate cost is the brain's processing cost, not an environmental cost). Closes `magic.md` Open Question Q1. Impl notes: each toggle Innate needs a `base_drain` config field (constants: `INNATE_HASTE_BASE_DRAIN = 13.33f`, `INNATE_SENSE_BASE_DRAIN = 6.67f`, `INNATE_FORTIFY_BASE_DRAIN = 3.33f`, mana/sec). Drain calc is `Mathf.Max(baseDrain * Mathf.Pow(0.96f, skillLevel), baseDrain * 0.25f)`. Per-frame mana subtraction: `mana -= drain(L) * delta`. Toggle-off and mana-exhaustion both halt drain. No density query in the drain path — keep Innate tick independent of `DungeonIntelligence` / floor state. Feeds SPEC-INNATE-STACKING-01 (next spec) which will decide concurrency rules. |
| SPEC-INNATE-STACKING-01 | Toggle-Innate concurrency rule — stacking vs one-active cap | Spec'd | P1 | Locked 2026-04-18. **Decision: Option A — Free stacking** (no hard concurrency cap). Full rule + stacking-combinations table + mid-game worked example in [magic.md §Stacking Rule (SPEC-INNATE-STACKING-01)](systems/magic.md#stacking-rule-spec-innate-stacking-01). Haste, Sense, and Fortify may run in any combination simultaneously; Armor does not participate (always-on, no drain). **Combined drain is additive** — at level 1, all three = **23.33 mana/sec** (13.33 + 6.67 + 3.33), which burns a Mage's 200-mana pool in **~8.6 seconds** with no regen. Pair-combinations: Haste+Sense 20.00/s (~10s), Haste+Fortify 16.67/s (~12s), Sense+Fortify 10.00/s (~20s). Self-balancing via mana economy — no UI cap needed. Mid-game worked example: level-15 Innate investment (~60% of base drain), 400-mana pool, 12 mana/s regen from Attunement → all-three net drain ~2.0 mana/s, effectively sustainable indefinitely; this is the build-identity payoff for Mages who invest in Attunement/INT. Warriors (60 pool) and Rangers (100 pool) naturally pick one or two per situation. Closes `magic.md` Open Question Q5. **Phase B now complete.** Impl notes: drain aggregation is a sum over active toggle-Innates each frame: `totalDrain = sum(drain(L_i) for i in activeToggleInnates)`. No concurrency guard in toggle-activation code — just pay the drain. Mana-exhaustion handling stays per-frame (if `mana <= 0` after subtraction, force all toggle-Innates off simultaneously; do not pick one to preserve). No new data fields — each Innate already has its own toggle state per SPEC-INNATE-MANA-COST-01. UI consideration: HUD mana-drain indicator should show summed drain when multiple Innates active (small numeric badge showing total mana/sec, not per-Innate breakdown — keeps HUD uncluttered). |

---

## Spec Roadmap Tickets — Phase J (Future / Deferrable)

Phase J is deliberately deferrable. Every item's decision on 2026-04-18 is "Deferred with gate" (not "not done yet"). Deferrals are themselves a lock — work does not reopen until the gate fires.

| ID | Title | Status | Priority | Notes |
|----|-------|--------|----------|-------|
| SPEC-ART-FX-01 | Bucket K effects redraw (sparks / hits / elemental FX) | Deferred | P3 | Gate: ISO-01 impl lands + iso-rendering pipeline stable. Deferred per PO 2026-04-17 direction. Reopens when `docs/systems/iso-rendering.md` is implemented and `asset-inventory.md` Bucket K has a current-state snapshot post-iso. No new spec file — deferral is the lock; reopening will author a fresh SPEC-ART-FX-01 against whatever the iso-era FX pipeline looks like. |
| SPEC-EXPORT-PLATFORMS-01 | Pick first distribution platform (itch.io / Steam / direct) | Deferred | P3 | Gate: playable MVP + PO signals interest in a store page. Rationale: different platforms need different build flags, icons, and marketing pages; picking pre-MVP is wasted effort. Reopens when game has enough content for closed-group playtest. |
| SPEC-ANALYTICS-BACKEND-01 | Pick telemetry stack (PostHog / GameAnalytics / homegrown / none) | Deferred | P3 | Gate: playable MVP + external playtesters. Telemetry without a player base has no patterns to surface. Reopens with SPEC-EXPORT-PLATFORMS-01. |
| SPEC-I18N-01 | Internationalization strategy | Deferred | P3 | Gate: first-platform decision + demonstrated localization market (non-English audience or publisher ask). Current state: English-first development; PS2P covers Basic Latin only per [SPEC-UI-FONT-01](ui/font.md); fallback-font registration already specced so non-Latin glyphs don't tofu when they're added. Reopens when a non-English market becomes a priority. |
| SPEC-AUDIO-01 | Music + SFX system | Deferred | P3 | Gate: explicit PO go-ahead. **PO has directly skipped audio** to manage spec-phase scope. On the long horizon post-MVP; no ETA. Revisit when PO signals. |
| SPEC-MULTIPLAYER-01 | Multiplayer support | Confirmed out of scope | P3 | Not a deferral — this is a design decision. The game is designed as single-player (dungeon narrative framing, save-slot-per-class, death-on-death mechanics all assume single-player). A multiplayer retrofit would require architecture-level changes. If multiplayer is ever revived as an option, this row updates to Deferred with a gate. |

---

## Spec Roadmap Tickets — Phase I (Movement & Input)

Phase I locks player movement feel + gamepad support + rebinding UI. Movement is the lead spec (confirms current instant-movement behavior); the other two build on it.

| ID | Title | Status | Priority | Notes |
|----|-------|--------|----------|-------|
| SPEC-MOVEMENT-ACCEL-01 | Instant player movement (no easing) | Spec'd | P1 | Locked 2026-04-18. See [movement.md §Acceleration](systems/movement.md). Confirms current `Player.cs:178` as spec: `Velocity = inputDir.Normalized() * MoveSpeed` on press, `Velocity = Vector2.Zero` on release. No Lerp/MoveToward anywhere in the velocity assignment. Rationale: Diablo 1 genre reference, precision-dodge feel for boss telegraphs, keyboard-first expectation. Haste multiplier + slow-zone multipliers both apply instantly (no ramp). Guardrail: block future PRs that introduce easing on Velocity. Movement.md Open Questions partially resolved here (easing) — gamepad routed to SPEC-GAMEPAD-INPUT-01. |
| SPEC-GAMEPAD-INPUT-01 | Xbox/PlayStation gamepad bindings | Spec'd | P2 | Locked 2026-04-18. See [gamepad-input.md](systems/gamepad-input.md). `project.godot` InputMap additions only — zero gameplay code changes needed since `Input.GetVector`/`Input.IsActionPressed` already abstract the source. Left-stick + D-pad → movement (deadzone 0.25); south/east/west/north buttons → action_cross/circle/square/triangle; D-pad up/left/right/down → skill_1/2/3/4; bumpers handle tab-cycling and stats-peek (non-overlapping contexts resolved by WindowStack.BlockIfNotTopmost); triggers handle Haste/Fortify toggles. Disconnect → auto-pause via `JoyConnectionChanged`. Single-player only (second controller ignored). Out of scope: rumble, DualSense haptics, right-stick (reserved for future cursor/camera spec). Accessibility swap-confirm/cancel Options toggle. Godot auto-renders PS/Xbox button labels via `InputEvent.AsText()`. |
| SPEC-INPUT-REBINDING-UI-01 | Input-rebinding Options sub-panel | Spec'd | P2 | Locked 2026-04-18. See [input-rebinding.md](ui/input-rebinding.md). Pause → Settings → Controls sub-panel. Row-per-action UI: action name + current-bindings chips + Add binding + Reset per row; Reset-all-to-defaults at bottom. Capture-mode modal for new binding (listens to next `_UnhandledInput`). Conflict handling: reassign-or-cancel prompt on cross-action collision; silent ignore on same-action duplicate. Escape reserved (cannot be bound — always cancel). Persistence: `user://input_bindings.cfg` ConfigFile, loaded at game start to override project.godot defaults. Preview-rebind with Escape-discards; only "Done" saves. Button labels use `InputEvent.AsText()` for controller-type correctness. Fully keyboard-navigable. Impl: new `InputBindings` autoload + `BindingCaptureDialog` (WindowStack-compatible modal). |

---

## Spec Roadmap Tickets — Phase H (UI Canonical Decisions)

Phase H locks the UI-wide constants every downstream UI spec inherits: font, scaling, HUD layout, feel effects. Font is load-bearing — every text surface uses it; changing it later means retouching every screen.

| ID | Title | Status | Priority | Notes |
|----|-------|--------|----------|-------|
| SPEC-UI-FONT-01 | Canonical font: Press Start 2P | Spec'd | P1 | Locked 2026-04-18. See [docs/ui/font.md](ui/font.md). **PS2P** (OFL 1.1 license — free for commercial use). Uppercase-only bitmap font with 8-px native cell. New `UiTheme.FontSizes` ladder aligns to integer-multiples of 8: Small=8, Body=Label=Button=16, Heading=24, Title=32, HeroTitle=48. Known tradeoff: Chief's long sentences need line-spacing ×1.5 + 50-char line cap + paragraph breaks in all-caps rendering. Mitigation rules specced. Integration via `UiTheme.FontFamily` preloaded FontFile, filter=Off for crisp edges. Locked alongside SPEC-UI-HIGH-DPI-01 (both require integer-scale discipline). Impl scope: `assets/fonts/PressStart2P-Regular.ttf` + `PressStart2P-OFL.txt` land; `UiTheme` adds `FontFamily`; size ladder updates; every UI screen gets the preloaded font. |
| SPEC-UI-HIGH-DPI-01 | Integer-scale Retina / 4K strategy | Spec'd | P1 | Locked 2026-04-18. See [docs/ui/high-dpi.md](ui/high-dpi.md). Godot project settings: `stretch/mode = canvas_items`, `stretch/aspect = keep`, `stretch/scale_mode = integer`, `textures/default_texture_filter = nearest`. Design resolution **1280×720** (1× reference); integer multiples only (2× = 1440p, 3× = 4K, Retina gets 2× + small letterbox). Fullscreen = borderless-windowed with largest-fitting integer scale. Options menu exposes integer multiples ONLY (no fractional options — they'd blur PS2P and pixel art). Letterbox beats cropping or stretching because it preserves every authoring pixel. Minimum supported resolution 1280×720; sub-minimum is best-effort. |
| SPEC-HUD-LAYOUT-01 | HUD element zone map + hotkeys | Spec'd | P1 | Locked 2026-04-18. See [docs/ui/hud-layout.md](ui/hud-layout.md). Zone map at 1280×720: HP orb bottom-left, MP orb bottom-right (Diablo style, 64px orbs flush to corners with 8px margin), skill bar centered between (4 slots × 56px), floor indicator top-left "F23" in Accent gold, stairs compass top-right (arrow + distance), buff bar top-center (horizontal icon row, contextual — hidden when empty), floating damage mid-center. **Stats panel accessed via Tab hold-to-peek** (overlay pauses game while held; release hides — faster than click-to-toggle-and-close). Hotkey map: Tab=stats-peek, Esc/P=pause, I=inventory, Q/E=tab-cycle in service menus, D=close-modal, 1-4=skill slots. NO user-toggleable HUD elements (HP/MP/skill-bar/floor/compass are core gameplay feedback; hiding them silently = bug surface). Impl: existing orbs + skill bar + stairs compass + floating text work; add buff bar (pull from `PlayerStats` active-effects) + Tab-overlay stats panel (extract from Pause Menu Stats tab). |
| SPEC-CAMERA-SHAKE-01 | Damage-proportional screen shake + flash | Spec'd | P2 | Locked 2026-04-18. See [docs/ui/camera-shake.md](ui/camera-shake.md). Triggers on exactly four events: player damage (proportional: `intensity = 4px * damage/max_hp`, `duration = 300ms * damage/max_hp`), player crit landed (flat 100ms / 1px), boss defeat (flat 500ms / 3px), boss phase-shift (flat 200ms / 2px, pairs with existing `FlashFx.Flash`). Does NOT trigger on regular player hits, enemy-on-enemy damage, pickups, level-ups. Decay is linear (not exponential — feels too aggressive at peak). Per-frame random jitter (smoothed = earthquake feel, wrong signal). Red-flash pairing for lethal-range hits (≥75% max HP, 30% opacity, 120ms). Overlapping shakes take **max** of (intensity, duration), not sum — additive compounds sickness risk. Accessibility "Reduce screen shake" toggle scales to 25%/50% (never zero — hit feedback still needed). Impl: new `CameraShake` node on Camera2D; subscribe to `Player.DamageTaken` + `Player.CritLanded` + `Enemy.Defeated` (with IsBoss check) + new `Enemy.PhaseShifted` signal. |
| SPEC-HITSTOP-01 | Hit-impact frame counts | Spec'd | P2 | Locked 2026-04-18. See [docs/ui/hitstop.md](ui/hitstop.md). Frame counts at 60 FPS reference: regular player hit=2f (33ms), crit=4f (67ms), damage taken=3f (50ms), phase-shift trigger=6f (100ms), boss defeat kill-blow=10f (167ms). **Durations computed as `frames/60` seconds** — framerate-independent. At 120 FPS display, same wall-clock duration renders as more frames; visual feel stays consistent across monitor refresh rates. Scope: game-world physics + AI + projectile travel pauses (`Engine.TimeScale = 0`); audio + particles + UI overlays continue (pausing audio reads as lag spike, not hitstop). Overlapping take **max**, not sum. Accessibility "Disable hitstop" zeroes all durations. Impl: new `Hitstop` autoload with `Trigger(durationSeconds)` static method; internally sets TimeScale=0 and uses un-scaled timer (`processAlways: true` — else the restoration timer itself pauses). Subscribe to the same four event classes as camera shake; values are lower but same trigger set. |

---

## Spec Roadmap Tickets — Phase G (NPC Dialogue & Service-Menu)

Phase G collectively unblocks NPC-ROSTER-REWIRE-01 (the code ticket that consolidates services to the 3-NPC roster). Voices spec is load-bearing for the other three.

| ID | Title | Status | Priority | Notes |
|----|-------|--------|----------|-------|
| SPEC-NPC-DIALOGUE-VOICES-01 | Voice conventions for Blacksmith / Guild Maid / Village Chief | Spec'd | P1 | Locked 2026-04-18. See [docs/flows/npc-dialogue-voices.md](flows/npc-dialogue-voices.md). Three voice profiles: Blacksmith (casual-warm, pioneer-smith-learning, short sentences), Guild Maid (clean-clipped, always full `{Class} Guildmaster`, neutral-polite), Village Chief (warm-formal, longer sentences, references village stakes). Five voice-distinction tests (length/formality/vocabulary/address/register) so any blind line is attributable to one NPC. Post-death + low-HP + high-value-transaction variants specced. Cross-NPC routing lines in each NPC's voice. No engine enforcement — voice rules are prose-level for dialogue authors (human or AI). |
| SPEC-VILLAGE-CHIEF-DIALOGUE-01 | Village Chief dialogue tree (quest-giver flow) | Spec'd | P1 | Locked 2026-04-18. See [docs/flows/village-chief-dialogue.md](flows/village-chief-dialogue.md). Six-state machine (first_meeting / idle / quest_offered / quest_in_progress / quest_complete / quest_declined) with full per-state dialogue text in Chief voice. Quest-body templating slots (`{quest_brief}`, `{quest_objective_summary}`, `{quest_reward_summary}`) fed by the quest system per-quest. Dialogue branches are narrow — every player option resolves to "open menu" or "close panel"; voice is wrapping, not branching narrative. Cross-NPC routing (Chief→Maid for banking, Chief→Blacksmith for craft). Post-death return line fires once per run. `firstMeetingDone` flag persists per save slot. |
| SPEC-BLACKSMITH-MERGED-MENU-01 | Blacksmith 4-tab service menu (Forge/Craft/Recycle/Shop) | Spec'd | P1 | Locked 2026-04-18. See [docs/ui/blacksmith-menu.md](ui/blacksmith-menu.md). Four tabs, default Forge: **Forge** (apply affixes per items.md §Affix System), **Craft** (material-to-item recipes), **Recycle** (uses SPEC-CRAFTING-QUALITY-LADDER-01 geometric ladder), **Shop** (caravan-stocked basics — absorbs the prior Guild Store). Persistent gold display; Q/E tab cycle; 1-4 direct tab jump; D/Esc close. Blacksmith voice fires on notable events only (first-craft, unfamiliar material, oddity). Shop-tab content source: `ItemDatabase` subset tagged `BlacksmithShopStock`. Impl: current `BlacksmithWindow.Open()` is single-service; rewrite to TabBar-hosting window — consider extracting `TabbedServiceWindow` base if Guild Maid also lands as multi-tab. Recycle formula call goes through `Crafting.RecycleItem` (no UI-side formula duplication). |
| SPEC-GUILD-MAID-MERGED-MENU-01 | Guild Maid 2-tab service menu (Bank/Teleport) | Spec'd | P1 | Locked 2026-04-18. See [docs/ui/guild-maid-menu.md](ui/guild-maid-menu.md). Two tabs, default Bank: **Bank** (storage + transfers in two-column layout, absorbs the prior Transfer tab functionality), **Teleport** (absorbs the prior `TeleportDialog` from the removed Teleporter NPC). No quest pickup — quest queries route to Chief in Maid's voice. Persistent gold + bank balance display. Q/E toggle; 1-2 direct; D/Esc close. **Partially supersedes [guild-window.md](ui/guild-window.md):** Store tab MOVES to Blacksmith (per SPEC-BLACKSMITH-MERGED-MENU-01); Teleport tab is NEW. Impl: rename `GuildWindow` → `GuildMaidWindow`, restructure to 2 tabs, migrate Store-tab logic to `BlacksmithWindow`, migrate Transfer-tab logic into Bank's two-column layout. Teleport tab uses existing `TeleportDialog` backing logic (only entry-point UI changes). |

---

## Spec Roadmap Tickets — Phase F (Per-Boss Instance Specs)

Each boss spec lives at `docs/world/bosses/<boss-name>.md` and follows the boss-adapted 8-section template from [docs/world/boss-art.md §2](world/boss-art.md). Each depends on its Phase E species spec for body plan + stat baseline, then applies boss multipliers (×5–8 HP, ×2–3 damage, ×0.8 speed, ×10 XP per reaction band), phase-shift triggers, and unique silhouette/aura cues.

| ID | Title | Status | Priority | Notes |
|----|-------|--------|----------|-------|
| SPEC-BOSS-BONE-OVERLORD-01 | Zone 1 capstone (floor 10) — 2-phase burst-down-fast | Spec'd | P2 | Locked 2026-04-18. See [docs/world/bosses/bone-overlord.md](world/bosses/bone-overlord.md). Skeleton species base. HP 300 / dmg 16 / speed 48 / XP 350. Phase 2 at 50% HP: 900ms ground-slam telegraph → 3-tile AOE. FlashFx.Flash(White, 120ms) on threshold. FORGE-01 Tier 1 unique (3-unique pool). Save-flag `floor10_boss_skeleton`. Arena-bound + 5%/s leash regen. |
| SPEC-BOSS-HOWLING-PACK-FATHER-01 | Zone 2 capstone (floor 20) — 2-phase burst-down-fast | Spec'd | P2 | Locked 2026-04-18. See [docs/world/bosses/howling-pack-father.md](world/bosses/howling-pack-father.md). Wolf species base. HP 450 / dmg 24 / speed 56 (optional 70 per art review) / XP 450. Phase 2 at 50% HP: summons 2× 1-HP phantom wolves (visual-only pack fantasy). FORGE-01 Tier 2 unique. Save-flag `floor20_boss_wolf`. |
| SPEC-BOSS-CHITIN-MATRIARCH-01 | Zone 3 capstone (floor 30) — 2-phase kite-from-range | Spec'd | P2 | Locked 2026-04-18. See [docs/world/bosses/chitin-matriarch.md](world/bosses/chitin-matriarch.md). Spider species base. HP 900 / dmg 45 / speed 52 / XP 600. 500ms web-wind-up telegraph. Phase 2 at 50% HP: summons 3× 1-HP spiderlings + ground-web 50% slow AOE for 2s. FORGE-01 Tier 3 unique. Save-flag `floor30_boss_spider`. |
| SPEC-BOSS-HOLLOW-ARCHON-01 | Zone 4 capstone (floor 40) — 2-phase caster, airborne | Spec'd | P2 | Locked 2026-04-18. See [docs/world/bosses/hollow-archon.md](world/bosses/hollow-archon.md). Dark Mage species base. HP 1260 / dmg 62 / speed 36 / XP 800. Airborne z-offset +24px. 600ms bolt wind-up, Phase 2 at 50% HP adds 1200ms ground-wave AOE with visible floor-glyph. FORGE-01 Tier 4 unique. Save-flag `floor40_boss_darkmage`. |
| SPEC-BOSS-WARLORD-FIFTH-01 | Zone 5 capstone (floor 50) — 2-phase ranged-kite | Spec'd | P2 | Locked 2026-04-18. See [docs/world/bosses/warlord-fifth.md](world/bosses/warlord-fifth.md). Orc species base (one of two bosses sharing the Orc body plan — see Volcano Tyrant for the other). HP 2040 / dmg 80 / speed 44 / XP 1100. 500ms axe-throw wind-up. Phase 2 at 50% HP halves throw cooldown + adds 700ms charge attack. FORGE-01 Tier 5 unique (10-unique pool). Save-flag `floor50_boss_orc`. |
| SPEC-BOSS-SCREAMING-FLIGHT-01 | Zone 6 capstone (floor 60) — **3-phase** close-the-gap | Spec'd | P2 | Locked 2026-04-18. See [docs/world/bosses/screaming-flight.md](world/bosses/screaming-flight.md). Bat species base (swarm-fused variant — **decision made: no separate species sub-spec, fusion is boss-only**). HP 3200 / dmg 114 / speed 88 (speed override) / XP 1400. Phase 1 airborne z+40 `ranged-kite`-inverted with 450ms dive wind-up. Phase 2 at 50% HP spawns 1-HP bat-fragment adds. Phase 3 at 25% HP: ground-collapse (z→0, 600ms z-lerp) + switch to `melee-chase`. **First 3-phase boss in the roster**; needs two one-shot flags. Phase-3 art: distinct ground-collapse frame required. FORGE-01 Tier 5 unique. Save-flag `floor60_boss_bat`. |
| SPEC-BOSS-IRON-GUT-GOBLIN-KING-01 | Zone 7 capstone (floor 70) — **3-phase** close-the-gap | Spec'd | P2 | Locked 2026-04-18. See [docs/world/bosses/iron-gut-goblin-king.md](world/bosses/iron-gut-goblin-king.md). Goblin species base. HP 3840 / dmg 126 / speed 48 / XP 1700. 700ms belly-slam telegraph. Phase 2 at 50% HP: 1000ms iron-slag vomit → persistent DOT zone for 4s. Phase 3 at 25% HP: turret-mode — stationary, 400ms projectile telegraph. **First-kill bundle is uniquely layered:** standard Tier 5 generics + Zone 1-3 species signatures (Bone Dust, Wolf Pelt, Chitin Fragment — the King has eaten all of them, fiction into mechanic). Phase-3 art: stationary turret pose required. FORGE-01 Tier 5 unique. Save-flag `floor70_boss_goblin`. |
| SPEC-BOSS-VOLCANO-TYRANT-01 | Zone 8 capstone (floor 80, deepest) — **3-phase** close-the-gap | Spec'd | P2 | Locked 2026-04-18. See [docs/world/bosses/volcano-tyrant.md](world/bosses/volcano-tyrant.md). Orc species base (second boss sharing the Orc body plan — fully differentiated from zone-5 Warlord via silhouette/aura/mechanics). HP 4800 / dmg 135 / speed 44 / XP 2000. Scale **2.2× (largest boss)**, 3 shadow-steps darker (deepest). 900ms hammer-swing telegraph. Phase 2 at 50% HP: three telegraphed fissure-lines → persistent lava tiles for 3s. **Phase 3 at 25% HP: body cracks open, passive heat-aura DOT within 2 tiles** — signature endgame mechanic, player must balance attack uptime against burn damage. FORGE-01 Tier 5 unique. Save-flag `floor80_boss_orc` (no collision with Warlord's `floor50_boss_orc` due to floor prefix). Phase-3 art: body-crack-open pose + 2-tile heat-aura visual required. **Phase F now complete.** |

---

## Spec Roadmap Tickets — Phase E (Per-Species Instance Specs)

Each species spec instantiates the 8-section contract in [docs/world/species-template.md](world/species-template.md) and lives at `docs/world/species/<species-id>.md`. All Phase E species specs are gated on Phase B (SPEC-MAGICULE-DENSITY-01) for stat scaling — the locked density curve lets each spec anchor stats at floors 3 / 28 / 75 without re-arguing density math.

| ID | Title | Status | Priority | Notes |
|----|-------|--------|----------|-------|
| SPEC-SPECIES-BAT-01 | Bat species spec — zone 1, Tier 1, Hide thematic (airborne) | Spec'd | P2 | Locked 2026-04-18. Reaction `kite-from-range`, AI `melee-chase` with swooping motion curve. Size Small (0.70×), hitbox 8px, z-offset +28px (airborne — iso Y-sort above grounded sprites). Silhouette: spread wings + lifted pose, readable as airborne from 8 tiles. Stats floor 3/28/75: HP 22/120/540, dmg 4/14/38, speed 90/105/120, XP 12/48/180. TTK 2-3 hits every floor. See [docs/world/species/bat.md](world/species/bat.md). Paired art: ART-14 (locked; assets in flight). Lifted from the worked example in species-template.md. |
| SPEC-SPECIES-SKELETON-01 | Skeleton species spec — zone 1, Tier 1, Bone thematic | Spec'd | P2 | Locked 2026-04-18. Reaction `close-the-gap`, AI `melee-chase` (plain contact — no shield-raise telegraph; that escalation is reserved for the Bone Overlord boss variant). Size Standard (1.00×, player-parity), hitbox 12px, z-offset 0 (grounded). Silhouette: upright armed-humanoid with sword-and-shield asymmetry readable at 8 tiles (the two hands must visibly differ). Stats floor 3/28/75: HP 34/185/820, dmg 5/17/44, speed 62/74/86, XP 14/56/210 — ~55% more HP and ~30% less speed than Bat at every floor (grounded/airborne trade). TTK 3-5 hits every floor. Drop-table row already locked in `MonsterDropTable.cs:40` (`material_sig_skeleton` 10%, `Bone` thematic, 60/20/20 generic split); spec documents it, no code change. Defines Bone Overlord base (SPEC-BOSS-BONE-OVERLORD-01 in Phase F). See [docs/world/species/skeleton.md](world/species/skeleton.md). Paired art: ART-SKELETON (not yet authored — current in-game sprite is placeholder at 0.70× scale, to be retuned to 1.00× when ART-SKELETON lands). |
| SPEC-SPECIES-WOLF-01 | Wolf species spec — zone 2, Tier 2, Hide thematic | Spec'd | P2 | Locked 2026-04-18. Reaction `pack-management`, AI `pack` (coordinated flank-split on aggro — anchor charges head-on while 2–4 pack-mates peel wide and arrive on player flanks within ~1.5s; lone survivor falls back to `melee-chase`). Pack cohesion radius 8 tiles; pack-aggro is shared (any member's aggro commits the whole visible pack). Size Large (1.25× — bottom of Large band, deliberately smaller than Orc so Wolves feel fast-and-dangerous rather than tanky), hitbox 15px, z-offset 0. Silhouette constraint: 3–5 Wolves at 8 tiles must read as a *pack* with visible spacing between units, not a blob — pack-management reaction only works if the player can count the group and see the flank-split. Stats floor 3/28/75: HP 40/215/960, dmg 7/24/66, speed 110/125/140 (outpaces player walk from first encounter), XP 20/78/290 — per-unit ~1.8× Bat reference; per-encounter threat multiplied by pack-of-3-5. TTK 1-2 hits per Wolf every floor (thin them fast or get eaten). Drop-table row already locked in `MonsterDropTable.cs:44` (`material_sig_wolf` 10%, Hide thematic — higher rate than Bat's 8% because packs kill in bursts; per-encounter signature yield matches Tier 1 single-spawn). Exempt pixels: eye glow + fang highlights (sub-node modulated Color.White, same technique as Bat). Defines Howling Pack-Father base (SPEC-BOSS-HOWLING-PACK-FATHER-01 in Phase F — boss inherits body plan, `pack` AI, scale band upgraded to Boss 1.8–2.2×). See [docs/world/species/wolf.md](world/species/wolf.md). Paired art: ART-14 (Bat/Spider/Wolf re-art batch; locked, assets in flight). |
| SPEC-SPECIES-SPIDER-01 | Spider species spec — zone 3, Tier 2, Hide thematic | Spec'd | P2 | Locked 2026-04-18. Reaction `burst-down-fast` (with `cautious-approach` as a secondary on repeat visits — once you know a corner is webbed, you slow down), AI `ambush` (stationary in corners until 120 px proximity + LOS; 200 ms body-rise telegraph → ~800 ms burst lunge at 160/185/210 px/s; falls back to plain `melee-chase` at standard idle speed 70/85/100 px/s if not killed within ~3s of reveal; 360 px leash allows sprint-past-and-return). Size **Small (0.75×)**, hitbox 9px, z-offset 0 (grounded) — deliberately small to leave "bigger and badder" silhouette headroom for the Chitin Matriarch. Tier 2 stats floor 3/28/75: HP 28/180/780 (between Bat T1 and Orc T3), dmg 7/22/62, split idle/lunge speed 70|160 / 85|185 / 100|210, XP 16/70/260. TTK 1-2 hits every floor (glass-cannon — kill it on reveal or eat the bite). Floor-3 numbers extrapolated for template consistency; earliest real encounter is zone 3 floors 21-30. Drop-table row already locked in `MonsterDropTable.cs:47` (`material_sig_spider` 8%, Hide thematic — silk/carapace drops as Hide mechanically). Silhouette constraint: 6-of-8 legs fanned, body <40% canvas height, wider-than-tall ground-hugging footprint; must read as arachnid (not blob) at 8 tiles so ambush failure feels like player error, not cheap spawn. Exempt pixels: 4-8-pixel eye cluster on cephalothorax (distinct from Bat's two-point eyes; drives "corner has eyes" recognition at aggro range — sub-node modulated Color.White same technique as Bat). Defines Chitin Matriarch base (SPEC-BOSS-CHITIN-MATRIARCH-01 in Phase F — boss inherits body plan, Hide thematic, scale floor 0.75× upgraded to Boss band 1.8–2.2×). Spider is the "fast ambush" slot in zone 3; Orc is the "slow heavy" slot. See [docs/world/species/spider.md](world/species/spider.md). Paired art: ART-14 (Bat/Spider/Wolf re-art batch; locked, assets in flight). |
| SPEC-SPECIES-ORC-01 | Orc species spec — zone 5, Tier 3, Ore thematic | Spec'd | P2 | Locked 2026-04-18. Reaction `cautious-approach`, AI `melee-chase` with a **600 ms heavy-swing telegraph** (raised-overhead pose + forward-arm flash; on-hit deals 1.5× contact damage). Contact outside the swing uses standard 700 ms hit cooldown per monsters.md. Tier 3 stats floor 3/28/75: HP 80/520/2,600 (highest per-floor HP in current roster), dmg 10/30/70, speed 55/62/70 px/s (slowest ground mob at every floor — "he gets there, you're in trouble"), XP 32/120/440. TTK 5-8 hits every floor per `cautious-approach` template. Floor-3 row is interpolation baseline only (Orcs spawn starting in zone 5 per `Constants.Zones`). Size **Large (1.40×)**, hitbox 17px, z-offset 0. Drop-table row already locked in `MonsterDropTable.cs:46` (`material_sig_orc` 10% / **Ore** thematic — metal-working/forged aesthetic drops as Ore); spec documents it. Silhouette constraint: top-heavy + visibly wider than player bounding box + hunched torso + asymmetric hands (swinging arm visibly larger than idle arm — pre-telegraph tell) — readable as "a wall with fists" at 8 tiles. Exempt pixels: tusk highlight (2px per tusk, bright-cream) + weapon glint (3px along blade/haft edge, cold-steel) — preserves "armed brute" identity at every level-gap (sub-node modulated Color.White same technique as other species carve-outs). **Base body plan for TWO future bosses:** Warlord of the Fifth (SPEC-BOSS-WARLORD-FIFTH-01, zone 5 — boss adds ranged-kite throwing-axes + iron-regalia aura) AND Volcano Tyrant (SPEC-BOSS-VOLCANO-TYRANT-01, zone 8 — boss is "deep-zone orc-form base" per Phase F roadmap, adds magma body-cracks + phase-3 heat-aura). Both inherit §1/§2/§5/§6/§7 from this species spec; both override §3 AI to layer boss-tier abilities. Phase F co-lock coordination required on any future Orc base rework. See [docs/world/species/orc.md](world/species/orc.md). Paired art: **ART-ORC** (proposed placeholder — no existing ART-* covers Orc redraw). |
| SPEC-SPECIES-DARKMAGE-01 | Dark Mage species spec — zone 4, Tier 3, Bone thematic | Spec'd | P2 | Locked 2026-04-18. Reaction `burst-down-fast` (primary) with `ranged-kite` secondary for a 1-tile retreat-step when player closes to 2 tiles; AI `caster` with **450 ms basic-bolt wind-up** (staff-tip purple glow + particle flare) and **900 ms AoE slow-field wind-up** (triggered at self-HP <40% OR 2+ targets in range — deliberately longer as the teachable-moment telegraph). Size Standard (1.00×, humanoid), hitbox 12px, z-offset 0 (grounded). Tier 3 stats floor 3/28/75: HP 18/95/420 (lower than Tier-1 Bat — "glass cannon" defined numerically), spell damage 9/32/95 (~2× Tier-1/2 contact-damage ceiling — the specific number that communicates "new threat class" on first encounter), move speed 55/65/75 (low on purpose — caster, not chaser), XP 22/85/320 (~1.75× Tier-2 comparable for the skill-check payoff). TTK 1-2 hits every floor (kill them instantly if reached; or interrupt the cast; or eat a half-HP spell). Drop-table row already locked in `MonsterDropTable.cs:49` (`material_sig_darkmage` **7%** / **Bone** thematic — rate is lower than T1/T2 because T3 signature drops are more valuable per drop). Silhouette constraint: upright thin-tall stance with hooded/skullcap head extending above head-line + raised staff or casting-hand visible during wind-up; must stand apart from the melee crowd at 8 tiles so the player can spot the caster inside 3s of encounter start. Exempt pixels: **purple eye glow + staff-tip glow (pulses brighter during the 450 ms telegraph) + hand-magic aura (visible during wind-up only, off otherwise)** — the three clusters that preserve species identity AND cast-telegraph legibility at any level-gap tint (sub-node modulated Color.White + AI-cast-state-driven visibility). Dark Mage is the first species whose combat is *read-react* rather than *position-trade*, and the first ranged Tier-3 threat in the zone progression. Defines Hollow Archon base (SPEC-BOSS-HOLLOW-ARCHON-01 in Phase F — boss inherits body plan, `caster` AI baseline, staff-tip glow family; boss phase-shifts + unique mechanics defer to that spec). See [docs/world/species/darkmage.md](world/species/darkmage.md). Paired art: **ART-DARKMAGE** (proposed placeholder — not yet authored; no existing ART-* covers zone-4 species redraw). |
| SPEC-SPECIES-GOBLIN-01 | Goblin species spec — zones 2+7, Tier 1, Ore thematic | Spec'd | P2 | Locked 2026-04-18. Reaction `pack-management`, AI `pack` (spread-to-flank when outnumbering the player, cluster-toward-sibling when outnumbered — rewarding the player for breaking the pack apart). Size **Small (0.80×)**, hitbox 10px, z-offset 0 (grounded). Tier 1 stats floor 3/28/75: HP 20/112/505, dmg 3/11/32, speed 80/92/104, XP 10/42/160. TTK 1-2 hits every floor. Stat anchors explicitly tuned against Bat (same Tier 1 / same TTK target) to invite the comparison: Goblin = slightly lower HP + damage, slower, 3–5 per ground cluster, threatens via flanking arcs; Bat = slightly higher HP + damage, faster, singles/pairs airborne, threatens via dive approach. Same per-unit frailty, different threat model. Drop-table row already locked in `MonsterDropTable.cs:43` (`material_sig_goblin` 10% / **Ore** thematic — scavenged scrap-metal gear drops as Ore). Silhouette constraint: cluster of 5 goblins must read as 5 distinct bodies at 8 tiles, never one blob — keep silhouettes narrow and vertically compact, arms close to body. Exempt pixels: skin (goblin-green must survive level-relative tint) + eye highlights; scrap-metal weapons and clothing are NOT exempt (level-tint still lands on majority of silhouette). Zone appearance: primary zone 2 (pack unit alongside Wolf Tier 2), reappears zone 7 where Iron-Gut Goblin King lives (zone 7 spawn counts lean toward high end 4–5 per cluster — lore-coded as "colonized the Goblin King's warren"). Defines Iron-Gut Goblin King base (SPEC-BOSS-IRON-GUT-GOBLIN-KING-01 in Phase F — boss inherits body plan + stat baseline; boss-specific rules not expanded here). See [docs/world/species/goblin.md](world/species/goblin.md). Paired art: **ART-GOBLIN** (placeholder — not yet authored; current sprite predates the pack-management silhouette constraint and needs redraw). |

---

## Spec Roadmap Tickets — Phase D (Combat Tuning Derivatives)

| ID | Title | Status | Priority | Notes |
|----|-------|--------|----------|-------|
| SPEC-MAGIC-COMBAT-FORMULA-01 | Mage spell damage: INT-only vs density-coupled | Spec'd | P2 | Locked 2026-04-18. **Decision: Option A — INT only, no density coupling.** Mage spell damage continues to scale only via `effective_int * 1.2%` per [stats.md §INT](systems/stats.md#int--intelligence-mage-primary); floor density from SPEC-MAGICULE-DENSITY-01 is explicitly excluded from the damage formula. Canonical "what density does and does not modify" table added to [magic.md §Density Formula](systems/magic.md#density-formula-spec-magicule-density-01). Stats.md spell-damage bullet updated to explicitly state "Spell damage is NOT modified by magicule density". Rationale: density manifests through the world (enemies, environment, dungeon AI) not player combat math; Mage-only density coupling would have scaled Mages past Warriors/Rangers since only Mages consciously channel environmental magicules. Keeps build identity stable and class balance predictable at depth. Impl: no code changes — current `effective_int * 1.2%` formula is the spec. Defensive note: future work on dungeon-pacts or scroll osmosis that *would* couple to density must explicitly state the scope (density-coupling on player combat math is the exception, not the pattern). |
| SPEC-COMBAT-03 | Per-archetype weapon tuning (Sword/Axe/Hammer, Shortbow/Longbow/Crossbow) | Blocked | P2 | Deferred per roadmap — waits until COMBAT-01 + COMBAT-02 impl lands. Needs real equipment to balance against. Not picking up in current sprint. |

---

## Spec Roadmap Tickets — Phase C (Skills/Abilities Completion)

All three Phase C specs resolved by reconciliation on 2026-04-18. The ARCHIVED `docs/systems/SKILLS_AND_ABILITIES_SYSTEMS.md` had stale TBD sections (Point Systems / Synergy Bonuses / Ability Affinity) that were already fully defined in LOCKED live specs. Reconciliation edits re-pointed the archive at the live specs; no new design work.

| ID | Title | Status | Priority | Notes |
|----|-------|--------|----------|-------|
| SPEC-SKILL-POINTS-RATE-01 | SP/AP rates + milestone schedule (reconciliation) | Spec'd | P2 | Locked 2026-04-18 via reconciliation. Canonical live spec: [point-economy.md](systems/point-economy.md) (already LOCKED). SP: 2/level, +1 at milestones (every 10th); AP: 3/level, +5 at milestones, plus combat-milestone AP (boss first-kill/floor clear/depth milestone) and use-based per-category AP trickle (~60/25/15 source split). XP-per-point formula `xp = 50 * (1 + target_level * 0.1)` same for both pools. The ARCHIVED SKILLS_AND_ABILITIES_SYSTEMS.md §Point Systems "needs adjustment for separate pools" TBD was already resolved by the separate-pools architecture in point-economy.md — this ticket just re-pointed the archive at the live spec. No new design decisions; no code edits; balance-tuning knobs remain open for playtesting per the live spec's own note. |
| SPEC-INNATE-SYNERGIES-01 | Innate synergies affect ALL abilities (reconciliation) | Spec'd | P2 | Locked 2026-04-18 via reconciliation. Canonical live spec: [synergy-bonuses.md §Innate Synergies](systems/synergy-bonuses.md#innate-synergies-affect-all-abilities) (already LOCKED). All four Innates (Haste/Sense/Fortify/Armor) have full Lv. 5/10/25/50/100 bonus ladders; Innate synergies affect ALL Abilities across all categories, not just their own children (unlike per-mastery synergies which scope to their parent Skill's children). Archive's "Innate synergies affect ALL abilities at threshold levels (details TBD)" line re-pointed at the live spec. Impl note for future: synergy application is a passive recalc at Skill level-up; Innate synergy applies globally across all equipped ability loadouts. No new design decisions; no code edits. |
| SPEC-MASTERY-THRESHOLD-FX-01 | Use-based cosmetic affinity tiers (reconciliation) | Spec'd | P2 | Locked 2026-04-18 via reconciliation. Canonical live spec: [ability-affinity.md](systems/ability-affinity.md) (already LOCKED). Four tiers: Familiar (100 uses), Practiced (500), Expert (1,000), Mastered (5,000). Cumulative cosmetic effects — no stat bonuses. Passive abilities track time active (1 sec = 1 use); toggle abilities track activations; Armor Innate explicitly excluded. Spec name is a misnomer in the roadmap ("MASTERY-THRESHOLD-FX" sounds like mastery synergy but is actually the use-based affinity system). Archive's "§Ability Affinity — TBD" section re-pointed at the live spec. No new design decisions; no code edits. **Phase C now complete.** |

---

## Audit Findings — 2026-04-17

Full report: [docs/audits/2026-04-17-full-project.md](audits/2026-04-17-full-project.md). Each ticket below is one finding — fix in its own branch / PR. Pick by priority.

| ID | Title | Status | Priority | Notes |
|----|-------|--------|----------|-------|
| AUDIT-01 | `Enemy.cs:53` — `SpawnRateModifier` mixed into enemy HP | Done | P1 | Fixed: removed `intel.SpawnRateModifier` from `hpMult` and `intel.AggressionModifier` from `spdMult` in `Enemy._Ready` (both were stat misuses per their docstrings — `DungeonIntelligence.cs:140, :151`). `SpawnRateModifier` now drives `Dungeon._spawnTimer.WaitTime`, refreshed per-tick in `OnSpawnTimerTimeout` so cadence tracks evolving pressure. `AggressionModifier` now drives `Enemy._hitCooldown.WaitTime`, refreshed per-attack in `DealDamageTo` for the same reason. Aggro range remains uniform (current architecture has no aggro range — enemies always chase the player). 470 unit tests green. |
| AUDIT-02 | `GodotFileSaveStorage.Write` — save failure not propagated | Done | P1 | Fixed: `ISaveStorage.Write` now returns `bool` (`FakeSaveStorage` always true; `GodotFileSaveStorage` false on `FileAccess.Open` null). `SaveManager.SaveToSlot` and `Save()` propagate the bool, log success/failure distinctly via `GD.Print`/`GD.PrintErr`. User-facing callers Toast.Error on failure: `PauseMenu` Back-to-Main, `DeathScreen` Quit, `Main.DoLoadTown`/`DoLoadDungeon` auto-saves; `DebugConsole` Force Save shows "Save FAILED" status. New unit test pins the bool contract. 471 unit + 11 integration tests green. Audit §2. |
| AUDIT-03 | `SaveManager.Save` — silent slot-0 overwrite when `CurrentSaveSlot` is null | To Do | P1 | PR #9 fixed the ClassSelect.Reset() path but the audit found two more sites that null `CurrentSaveSlot` before a save fires: PauseMenu "Back to Main Menu" and DeathScreen quit. Same root cause as PR #9, broader scope. Either: (a) make `Save()` refuse when slot is null instead of falling through to 0, OR (b) audit every site that nulls `CurrentSaveSlot` and confirm no autosave can fire after. Audit §3. |
| AUDIT-04 | `MagiculeAttunement.ImportState` — corrupt save inflates points | To Do | P1 | `TotalPoints` is reconstructed from `_clearedFloors.Count` without filtering by `> UnlockFloor`. A corrupt or pre-unlock-era save can inflate the player's attunement points. Fix: rebuild `TotalPoints` by iterating `_clearedFloors` with the unlock filter, mirroring how points are awarded during play. Audit §4. |
| AUDIT-05 | `PauseMenu.OnShow` — synchronous `Free()` of in-tree node + dead `_tabs` | Done | P2 | Fixed in PR #39 (`058edce`). `PauseMenu.OnShow` now `QueueFree`s the prior tab panel instead of `Free()`, and the dead `_tabs` assignment in `BuildContent` was removed. No behavior change for users — this was a latent GC/lifetime cleanup. |
| AUDIT-06 | `Toast.Show` — SceneTreeTimer holds reference to potentially-dismissed toast | Done | P2 | Fixed in PR #38 (`5c5ce81`). `DismissToast` gated on an `IsInstanceValid` check + a per-toast `dismissed` flag, so the auto-dismiss `SceneTreeTimer.Timeout` callback becomes a no-op when the toast has already been removed via `MaxVisible` overflow. Unit tests added for the double-dismiss scenario (source-level regression guard via literal-aware brace counter). |
| AUDIT-07 | `Dungeon.OnEnemyDefeated` — `async void` with side effects before tree guard | Done | P3 | Fixed in PR #40 (`c1eba42`). `IsInsideTree` guard hoisted above the `_killCount` increment so the counter no longer ticks when the handler fires during scene unload. Still `async void` (Godot signal handlers require it) — the race surface is closed by the guard order, not by the method signature. |
| AUDIT-08 | `Constants.PlayerStats.GetMaxHp` — O(level) loop on every `StatsChanged` | Done | P3 | Fixed in PR #41 (`5c0b159`). `GetMaxHp` rewritten as closed-form `(long)StartingHp + 8L * level + (long)level * level / 4L` via new private `GetMaxHpLong` helper + public `GetEffectiveMaxHp(level, bonus)` wrapper with Int64 saturation. All four callsites (GameState, StatAllocDialog, PauseMenu, DebugConsole) routed through `GetEffectiveMaxHp`. Boundary tests cover level 0, negative levels, and the Int64→Int32 overflow clamp. |
| AUDIT-09 | Spec-vs-spec drift: `item-generation.md` (5-bracket) vs `depth-gear-tiers.md` (7-bracket) | Spec'd | P2 | Resolved 2026-04-18 by SPEC-RECONCILE-BRACKETS-01. Option A — deleted the stale §Quality Distribution by Floor Range subsection (5-bracket / 3-quality table) from `docs/systems/item-generation.md`; replaced with a one-line pointer to `depth-gear-tiers.md` §Drop Rates as the canonical source for quality rolls. The 5-bracket system remains canonical for catalog tier assignment and material tier selection (unchanged). No code changes — code already matched the 7-bracket canon (`DepthGearTiers.RollQuality` and `MonsterDropTable.FloorToTier`). Audit Spec Drift §4. |
| AUDIT-10 | `AffixDatabase.GetMaxTier` — claims tiers 5/6 exist but registry only has 1–4 | Done | P2 | Landed in PR #37 (`df74f1b`). 16 new T5 + T6 affixes registered per SPEC-AFFIX-TIER-LADDER-01 (8 families × 2 tiers: keen, vicious, sturdy, warding, striking, ruin, bear, swiftness). Total registry now 44 (28 T1–T4 + 16 T5–T6). 38 new `AffixDatabaseTests` cover min-item-level gates, tier value monotonicity, cost ladder, registry count. `items.md` footnote updated. No logic changes — `GetMaxTier` already returned 5/6 at correct thresholds. |
| AUDIT-11 | `Crafting.RecycleItem` — quality bonus missing Masterwork/Mythic/Transcendent cases | Done | P2 | Landed in PR #36 (`88dfb4b`). `Crafting.RecycleItem` now covers all 6 quality tiers per SPEC-CRAFTING-QUALITY-LADDER-01: Normal 0, Superior +25%, Elite +50%, Masterwork +100%, Mythic +200%, Transcendent +400% of `baseGold`. `CraftingTests.RecycleItem_AppliesQualityBonus` parametrized over all 6 tiers. Out-of-scope follow-up (recycle-returns-materials) remains open if ever raised. |
| AUDIT-12 | Test gap: `DungeonIntelligence` (199 LOC, time/decay/pressure) | To Do | P1 | Completely untested. Time-based decay, pressure score thresholds, modifier curves all need coverage before more endgame work touches them. Audit Test Gaps. |
| AUDIT-13 | Test gap: `AffixDatabase`, `ItemDatabase`, `ClassAttacks`, `SpeciesConfig`, `FloorGenerator` | To Do | P2 | None directly tested. ItemDatabase needs uniqueness + slot-coverage assertions to catch catalog-rename breakage early. Audit Test Gaps. |
| AUDIT-14 | Silent-no-op tests in `QuestSystemTests` (lines 67/102/126/146) | To Do | P2 | Multiple tests early-return on RNG misses, so they pass without asserting their core invariant. Same pattern PR #9 fixed in `EquipmentSetTests` — replace early-return with seeded-deterministic setup. Audit Test Gaps. |
| AUDIT-15 | Coverage Gate broken — empty coverage XML on every CI run | Done | P2 | Fixed: added `coverlet.runsettings` at repo root with `<IncludeTestAssembly>true</IncludeTestAssembly>` (required because the test projects compile production source in directly via `<Compile Include>` rather than referencing a separate logic project, so coverlet's default test-assembly skip produced empty reports) and `<ExcludeByFile>**/tests/**</ExcludeByFile>` (so the metric reflects production-source coverage, not test-source self-coverage). CI workflow updated: unit-tests job uses `--settings coverlet.runsettings`; integration-tests job no longer collects coverage (would double-count the same production source compiled into both test assemblies and dilute the metric); Coverage Gate filters to `+DungeonGame.Tests.Unit` and the threshold is set to 75% (current actual ~79%, leaves a small cushion for in-flight changes). Build/test/gate verified end-to-end locally. |
| AUDIT-16 | `Enemy.DealDamageTo` invincibility no-op leaves cooldown unstarted | To Do | P2 | When player is invincible, `DealDamageTo` returns early without starting `_hitCooldown`. If player stays inside `HitArea` for the entire invincibility window, `BodyEntered` won't refire and the cooldown never starts → enemy never re-attempts to deal damage until player leaves and re-enters the hit area. Found by Copilot R3 on PR #13 (out of scope for AUDIT-01 since it's pre-existing behavior, not introduced by AUDIT-01's WaitTime refresh). Fix: always start `_hitCooldown` on invincibility-skip path (so `OnHitCooldownTimerTimeout` can re-poll the overlap and re-attempt damage when invincibility ends). |
| AUDIT-17 | UI Tests CI job red on every PR + main — `MissingMethodException` for `FileAccess.GetAsText` and `CanvasItem.DrawString` | Done (re-triaged 2026-04-20) | P1 | **Original issue superseded.** The specific `MissingMethodException` for `FileAccess.GetAsText` / `CanvasItem.DrawString` is gone as of the Godot 4.6.2 sync in PR #26 (`d796419`). Post-re-triage CI on the UI Tests job runs green on every recent main push; the merge chain from 2026-04-19 onward is evidence. A distinct failure on the E2E / GdUnit4 job surfaced after the 2026-04-20 GODOT_BIN fix (PR #44) — GdUnit4's dotnet-test runner now finds the Godot binary but hits a `System.TimeoutException` waiting on GodotGdUnit4RestClient during scene bootstrap. That's a separate issue tracked as AUDIT-18 below, not a resurrection of this row. |
| AUDIT-18 | E2E / GdUnit4 job times out during scene bootstrap (post-GODOT_BIN fix) | To Do | P2 | Discovered 2026-04-20 during AUDIT-17 re-triage. After PR #44 added the missing `GODOT_BIN` env var, the E2E job on main now progresses past the "runtime not configured" error and into scene bootstrap, where `System.TimeoutException: The operation has timed out.` fires inside GdUnit4's `GodotGdUnit4RestClient`. Symptom: the REST client can't complete its handshake with the headless Godot process within the 5-minute `--settings .runsettings-ci` timeout. Likely candidates: (a) headless Godot failing to stand up the REST endpoint due to autoload-compile errors logged during bootstrap (see run log `scripts/autoloads/GameState.cs` / `EventBus.cs` / `SaveManager.cs` "not compiling" errors), (b) the GDUIComponentLibrary plugin parse error on `icon-select.png`, (c) missing X virtual display setup. Lower priority than AUDIT-17's dev-impact rating because the UI Tests job is green and covers the keyboard-nav surface that matters; E2E is still scaffolded-not-wired per TEST-04 anyway. |

---

## Cross-Cutting Policy

| ID | Title | Status | Priority | Notes |
|----|-------|--------|----------|-------|
| SPEC-AI-SAFETY-01 | Generative-AI safety policy (legal compliance) | Spec'd | P1 | Locked 2026-04-20 per PO directive responding to new generative-AI regulatory requirements. [docs/conventions/ai-safety-policy.md](conventions/ai-safety-policy.md): §1 prohibited-content taxonomy (7 hard-reject categories + narrow nudity allowance + gray-zones escalate to PO), §2 dev-time guardrails (prompt + output classification + provenance audit trail at `docs/assets/ai-provenance.ndjson`), §3 runtime guardrails (future — MVP has no runtime AI), §4 in-app reporting (F10 global + per-surface context menu; local-only in MVP via `user://ai_reports.ndjson`, cloud opt-in post-launch), §5 weekly triage + filter-update loop, §6 compliance-by-law table, §7 audit artifacts list, §8 roles, §10 **five** impl follow-ups (Report UI, credits sweep, triage stub, pipeline-spec back-refs, provenance log init). **MVP blocker:** POL-AI-REPORT-UI-01 must ship in the **first shipped build** containing any player-visible AI-generated surface (dev-time static AI assets count); internal/dev-only builds do not gate on this ticket. Trigger matches §4.1 language. |

---

## P3 -- Future

| ID | Title | Status | Notes |
|----|-------|--------|-------|
| FUT-01 | Gamepad support | To Do | Input Map actions defined; no joypad events added |
| FUT-02 | Key rebinding UI | To Do | |
| FUT-03 | Desktop export (macOS/Windows/Linux) | To Do | |
| FUT-04 | Monster trophy crafting | To Do | |
| FUT-05 | Evolving items (grow through use) | To Do | |
| FUT-06 | Synergy ring system (10 ring slots) | To Do | |
| FUT-07 | Dungeon Weather (daily magicule fluctuation) | To Do | |

---

## Test Summary

| Suite | Command | Notes |
|-------|---------|-------|
| Unit tests (xUnit) | `make test-unit` | Run `make test-unit` for current count |
| Integration tests (xUnit) | `make test-integration` | Cross-system flow tests |
| Full-run integration | `make sandbox-headless SCENE=full-run` | End-to-end play session |
| Sandbox scenes (14) | `make sandbox-headless-all` | Headless checks per system |
| E2E (GdUnit4) | `make test-gdunit` | Scaffolded, pending wiring (TEST-04) |

---

## File Counts

| Category | Count | Location |
|----------|-------|----------|
| Scene files (.tscn) | 8 | `scenes/` |
| C# scripts | 87 | `scripts/` (includes autoloads/, ui/, logic/) |
| Input actions | 12 | `project.godot` [input] section |
| Autoloads | 3 | GameState, EventBus, SaveManager |
| Item definitions | 8 | `ItemDatabase.cs` |
| Enemy species | 7 | Skeleton, Goblin, Bat, Wolf, Orc, Dark Mage, Spider |
| Player classes | 3 | Warrior, Ranger, Mage |
| Projectile sprites | 8 | `assets/projectiles/` |
| UI orb assets | 2 | `assets/ui/` (HP red, MP blue) |
| Zone tilesets | 7 | `assets/tiles/` |

---

## Milestone Targets

| Milestone | What | Tickets |
|-----------|------|---------|
| **Visual Foundation** | One tile, one sprite, one room, movement, camera | VIS-01 through VIS-06 -- ALL DONE |
| **Playable Prototype** | Combat, HUD, death, game loop, state management | PROTO-01 through PROTO-06 -- ALL DONE |
| **Feature Complete** | All NPCs, classes, skills, death flow, quests | SYS-01 through SYS-10 |
| **Endgame** | Pacts, attunement, adaptive dungeon, gear tiers | END-01 through END-05 |
| **Polish** | Audio, animations, shaders, zone themes | POL-01 through POL-04 |
| **Ship** | Export, gamepad, key rebinding | FUT-01 through FUT-03 |
